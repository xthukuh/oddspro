# Force LF regardless of a committer's core.autocrlf - CRLF in a shebang
# script (scripts/pipeline-cron.sh) breaks bash on the Linux cPanel host
# ("bad interpreter" from the trailing \r).
*.sh text eol=lf
.cpanel.yml text eol=lf
