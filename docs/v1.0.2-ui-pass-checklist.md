# v1.0.2 UI pass — checklist

Presentation + UX pass on `web/` (branch `dev`). Approved design in session 2026-07-13.
No data/sort-logic/API changes. Suite baseline: 316.

Legend: [ ] pending · [~] in progress · [x] done

## A. SHOW_DETAILS (env-configurable, default ON)
- [x] `web/src/details.js` — `SHOW_DETAILS` from `import.meta.env.VITE_SHOW_DETAILS` (default on; `0`/`false` disables)
- [x] `TipPopover.jsx` — drop local `SHOW_INTERNALS`, use imported `SHOW_DETAILS`
- [x] `MagicMenu.jsx` — hide intro prose + STATS_TITLE tooltip + per-strategy stat line + sample footer when off (names + toggles only)
- [x] `.env` + `.env.example` — documented `#VITE_SHOW_DETAILS=1`

## B. One consolidated sticky cell + hover-bleed fix
- [x] Replace separate pinned Score/Tip(/Magic) duplicates with ONE pinned summary cell (score/tip/magic stacked, color-coded), anchored to Tip scroll-out
- [x] Opaque hover on sticky cells (layer `--fill` over the opaque tint, don't replace it)
- [x] Content-fit width (no fixed px; measured hysteresis) + 10px text - browser-verified 148px→90px

## C. Remove Goals column (keep filterable)
- [x] `baseColumns.js` drop `goals`; `columns.js` EXTRA_BASE_LABELS gains `goals`; DataTable removes goals HEADER_META/CELL_TITLES/_cell branch

## D. Filter key/type/example in header tooltip
- [x] Pure `filterHint(col)` (key · type · example expr) in `columns.js` + test; DataTable header `title` appends it

## E. Purple accent + betika teal + Tip-as-link
- [x] `index.css` `--accent` #5856dc/#8B89F0, `--accent-soft`, `--provider-b` teal
- [x] Tip main `tip_market` span gets `text-accent` (real cell + sticky summary)

## G. Selection-count badge on Select header
- [x] `selCount/total` (collapse to one value when equal, hidden at 0), accent-styled; SELECT_W 40→44

## H. Per-node filter enable checkbox (default on)
- [x] `FilterBuilder.jsx` — `enabled` flag on cond/group/expr + checkbox UI
- [x] Skip disabled in `filterExpr.evalGroupResolved`, `filterValues.splitFilters`, `filterValues.conditionCount` + tests

## I. Responsiveness (magic sort / bulk filter lag)
- [x] `App.jsx` — `useTransition` around chain + filter updates; delayed spinner via `loading`

## Verify
- [x] `npm run build:web` clean · `npm test` green (+ new) · browser smoke-test
