# v1.1.0 Implementation Plan & Checklist

Users · Auth · Sessions · Roles · Admin · SMS/OTP · Sync · Data-Viz Lab
Full design: `C:\Users\User\.claude\plans\polymorphic-whistling-dawn.md`

Status legend: ⬜ pending · 🟦 in progress · ✅ done

---

## Phase 0 — Bug fixes (branch `fix/net-retry-humanpow` → `main`)
- ✅ Bug #2: `src/db/net-rules.js` `isRetryableNetworkError` + `tests/net-rules.test.js` (6 tests, verified retry self-heals)
- ✅ Bug #2: wrap `src/apisports.js` line-96 GET in `withRetry` (network predicate)
- 🟦 Bug #1: `.env.production` template (HUMAN_POW_ENABLED ↔ VITE_HUMAN_POW parity)
- ⬜ Bug #1: `.env.example` paired-flag comment + `docs/DEPLOYMENT.md` note; verify `scripts/package-deploy.js`
- ⬜ `npm test` green; commit; (ask before merging to `main`)

## release/1.1.0 (branch off `main` after Phase 0)

### Phase 1 — Data model (3 migrations) ✅ (batch 11 applied + verified on live DB)
- ✅ `src/auth-rules.js` PIN hashing (scrypt+salt+pepper) + `tests/auth-rules.test.js` (7 tests)
- ✅ `20260714000001_users_sessions.js` (+ seeded admin id=1 verified: PIN auths, `must_change_pin`)
- ✅ `20260714000002_otp.js` (`otp_codes`)
- ✅ `20260714000003_settings_user_prefs.js` (`settings`, `user_prefs`)
- ✅ `config.js`: `PIN_PEPPER` + `ADMIN_SEED_PIN`

### Phase 2 — SMS subsystem (Bonga, swappable, zero new backend deps)
- ⬜ `src/config.js` SMS/BONGA/OTP knobs
- ⬜ `src/db/sms-rules.js` (pure) + `tests/sms-rules.test.js`
- ⬜ `src/sms/index.js` provider seam + `src/sms/bonga.js`
- ⬜ `src/index.js` `sms` CLI action

### Phase 3 — Auth service + routes + middleware
- ⬜ `src/config.js` AUTH/PIN/SESSION knobs
- ⬜ `src/auth-rules.js` (node:crypto pure) + `tests/auth-rules.test.js`
- ⬜ `src/authlimit-rules.js` + `tests/authlimit-rules.test.js`
- ⬜ `src/auth.js` (knex loader)
- ⬜ `src/server.js` `requireAuth`/`requireRole`/`requireVerified`/`optionalAuth` + `/api/auth/*` routes

### Phase 4 — Dynamic settings service
- ⬜ `src/db/settings-rules.js` (pure) + `tests/settings-rules.test.js`
- ⬜ `src/settings.js` late-read override service
- ⬜ `/api/settings` + admin `GET/PUT/DELETE /api/admin/settings`
- ⬜ refactor `magic.js#safePolicy` + refresh cooldown to read via service

### Phase 5 — Frontend auth (SessionProvider, phone, verify gate, avatar)
- ⬜ `web/package.json` add `react-phone-number-input` + `libphonenumber-js`; bump 1.1.0
- ⬜ `web/src/auth/*` (SessionProvider, AuthGate, SignUp/SignIn/VerifyPhone, PhoneField, useCooldown, sessionToken)
- ⬜ `web/src/components/AvatarMenu.jsx` + icons + OverflowMenu session rows
- ⬜ `web/src/api.js` auth wrappers + token attach; `web/src/main.jsx` wrap order

### Phase 6 — Admin dashboard + data-viz lab (Recharts)
- ⬜ `web/package.json` add `recharts`
- ⬜ `src/db/lab-rules.js` (pure) + `tests/lab-rules.test.js` + `src/lab.js`
- ⬜ `/api/admin/lab/features` + `/api/admin/lab/data`
- ⬜ `web/src/admin/*` (AdminPanel, SettingsEditor, DataLab, charts)

### Phase 7 — Cross-device prefs sync (LWW)
- ⬜ `src/db/prefs-rules.js` (pure) + `tests/prefs-rules.test.js`
- ⬜ `/api/prefs` GET/PUT; `web/src/auth/prefsSync.js`; configSnapshot device-key exclusion

### Phase 8 — Guest vs normal gating
- ⬜ `src/db/access-rules.js` (pure) + `tests/access-rules.test.js`
- ⬜ `src/db/records.js` + `src/server.js` `optionalAuth` + redaction/date-ceiling
- ⬜ `web/src/App.jsx` guest date clamp + detail hide

### Phase 9 — Docs, version bump, verification, merge
- ⬜ version → 1.1.0; `.env.example`; `CLAUDE.md`; `README`/`DEPLOYMENT`
- ⬜ full `npm test`; E2E browser verification; scratch-DB migration run
- ⬜ PR `release/1.1.0` → `main` (on user request)

---

## Open escalations (surface during impl)
1. `PIN_PEPPER` becomes effectively required — validate loudly at `auth.js` init.
2. Bonga wire format (MSISDN + urlencoded vs multipart) — verify live.
3. SMS-enabled-but-creds-unset → fail-closed 500.
4. Prefs per-write auto-push deferred (needs central localStorage write helper).
5. Include any uncommitted working-tree changes in the appropriate phase commit.
