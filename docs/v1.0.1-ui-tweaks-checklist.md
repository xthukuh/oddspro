# v1.0.1 UI/UX Tweaks — Implementation Checklist

> **Source of truth** for the v1.0.1 polish pass. Updated religiously per checkpoint.
> Plan: `~/.claude/plans/synchronous-fluttering-petal.md`. Statuses: ⬜ pending · 🔄 in progress · ✅ done · ⏸ blocked.
> Suite baseline: **286/286** (was 196; +D/E/F, +9 H retry-rules, +4 I legPicks) — must stay green at every commit. Phases A→**F all done (2026-07-11)**; **Phase H (sticky Tip + refresh + deadlock)**, **Phase I (audit-extras completion)** and **Phase J (popup dismiss, compact footer, betslip guard, help/copy polish)** done (2026-07-11); **the optional backlog is cleared — Phase G is the only pending phase**, and it runs only after the user says **"we are ready for deployment."**

---

## Step 1 — Bootstrap & visual audit
- ✅ Create this checklist file
- ✅ Confirm local app runs (API :3001, scheduler off, MariaDB up, data present)
- ✅ Run ui-ux-designer visual audit — 8 screenshots (desktop+mobile: table/settings/filters/betslip/overflow) at 1536×774 & 390×740; agent returned 13 optimizations + 8 risk-flags
- ✅ Present audit findings for approval; fold approved extras into this checklist

### Approved audit extras (fold into phases)
- ✅ **AX1** Tip win-% visual weight — done in Phase B (`R26a / AX1`, `_pctClass` 3-tier), extended to the sticky Tip in Phase H
- ✅ **AX2** Main-table hit/miss ✓/✗ icon+color redundancy — done in Phase B (confirmed present, `_tick`)
- ✅ **AX3** Non-positive/`0.00` price → void/stale styling — done in Phase B (`_marketCell` ✕ void)
- ✅ **AX4** Provider badges betpawa/betika distinct hues — Phase I (blue/violet tokens; both themes verified)
- ✅ **AX5** Settings checkbox rows fully tappable ≥44px — Phase I (`Toggle` `min-h-[44px]`; measured 44px)
- ✅ **AX6** Betslip result line: outcome pill separated from metrics; one good/caution/bad scale — done in Phase D (`OutcomePill`, D4)
- ✅ **AX7** Betslip tip-name tap-to-expand/wrap (near-dupes) — Phase I (tap toggles truncate↔wrap)
- ✅ **AX8** Horizontal-scroll edge-fade affordance on the table — Phase I (right-edge fade, hides at end)
- ✅ **AX9** Zero-result empty state (icon + "clear a filter" shortcut) — Phase I (magnifier icon + context message + Clear-filters button)
- ✅ **AX10** Mobile sheets reach true 100dvh (footer peek fix) — Phase I (verified: existing header+scroll-body+pinned-footer keeps the footer reachable at 390px; no code change needed)
- ✅ **AX11** stale-grey vs backdrop-dim / active-icon / hamburger — icon half done in Phase A4 (`IconMenu` 3 equal bars vs `IconFilter` decreasing); stale-grey (grey text) vs backdrop-dim (blur+opacity overlay) already visually distinct, active-toolbar-icon state = `navBtnActive` accent bg — verified adequate, no change needed
- ✅ Guardrails: ≥44px betslip "+" hit-zone (D2) · auto-create-slip only on explicit add w/ zero slips (D2) · non-color active-slip marker (D3 border+ring) · demote id in Start (R30a) · Filters "Apply" closes + live count (Phase C) — all verified present

### Decisions
- **Filters** → progressive disclosure: basic inline, regex/nested-groups/expression behind an **Advanced** toggle; inline validation never silently 0-results.
- **Mobile card-layout row** → DEFERRED to v1.0.2 (v1.0.1 does the column-pruning + table changes only).
- **Theme default Light** kept per user; System one tap away.

---

## Phase A — Overlay & layering foundation ✅ (committed, suite 201/201)
- ✅ **A0** Extracted shared `web/src/useAnchoredPanel.js` (from MultiSelect+ReorderList dup) — adds Escape (document-bubble + stopPropagation so a dropdown inside a Sheet doesn't also close it). (Dropdowns keep proven pointerdown dismiss per R2's own "dropdowns just auto-dismiss" note — no ClickCatcher rewrite needed; steal already present on modals/toolbar-popups/TipPopover.)
- ✅ **A1** `Sheet` `dismissable` prop + `PinToggle` (IconPin) in all Sheet modals (Settings/Help/Magic/Betslip; Filters deferred to its Phase C rebuild); Betslip migrated onto `Sheet` (default **pinned**); Escape added to MultiSelect/ReorderList (hook)/CalendarPopover/OverflowMenu — *(R2)* — verified: pinned ignores backdrop, unpinned closes.
- ✅ **A2** `web/src/zLayers.js` z-scale source-of-truth applied to Sheet/Overflow/Calendar/Tip/Tooltip/dropdowns; sticky-column bleed fixed (tr bg fallback → `bg-surface` = pinned-cell parity) — *(R3, R4)* — verified no bleed on H-scroll.
- ✅ **A3** Removed table wrapper `rounded-xl`; reordered-sticky-column gap fixed (pin duplicate forced to exact measured real-column width so adjacent pins butt together) — *(R1, R4)*. (dropdown-panel top peek-through revisited if seen again.)
- ✅ **A4** `IconMenu` (3 equal bars) replaced ⋯ overflow trigger — *(R2)* — verified on mobile.
- ✅ Commit Phase A · suite green (201/201) · build clean

## Phase B — Table content cleanup (core committed; tip-cell R26a/AX1 next)
- ✅ **B1** Start column → time-only (`_hm`); full datetime → tooltip — *(R21)*
- ✅ **B2** Removed ID column; id greyed/small prefix in Start cell + api_id/fixture context merged into Start tooltip; `api_id` stays filterable via EXTRA_BASE_LABELS — *(R22)*
- ✅ **B3** Removed Updated + Locked columns → appended to Status tooltip; both stay filterable — *(R21)*
- ✅ **B4** Removed-column labels preserved for filtering; FULL filter-field completeness audit done in Phase C (every base/market/team-stat column + home/away team now in the filter field list) — *(R14, R23)*
- ✅ **AX3** Non-positive `0.00` price → ✕ void state (verified on Canberra row)
- ✅ **AX2** Main-table tip hit/miss already icon+color (✓/✗) — confirmed present
- ⬜ **AX4** Provider badge distinct hues — DEFERRED (needs a palette-token decision; betpawa green vs betika teal already differ subtly)
- ✅ **R26a / AX1** Tip cell: top-3 picks stacked (chosen bold+full, runners-up smaller/muted, numbered 2./3.), each with ✓/✗ settled from score via `tipHit`; chosen % weighted by confidence tier (`_pctClass`: ≥70% accent-bold) — verified
- ✅ Commit Phase B core + tip-cell · suite green (201/201)

## Phase C — Filters overhaul
- ✅ **C0** Pure `web/src/filterExpr.js` engine (recursive AND/OR groups, safe `match`/`not-match` regex, `$row[...]` recursive-descent expression parser — NO eval; helpers `contains`/`in`/`raw`/`lower`/`upper`/`abs`/`num`) + `tests/filter-expr.test.js` (22 tests). `filterValues.js` now DELEGATES to it (`rawValue`/`filterRows` — one source of truth, no drift). Suite 223/223 — *(R16, R17)*
- ✅ **C1** All columns filterable incl. new `home_team`/`away_team` (server `BASE_FIELDS` + `EXTRA_BASE_LABELS`); low-cardinality dropdown pickers — verified in browser (Match info group now lists Home/Away Team) — *(R14)*
- ✅ **C2** Type-aware value controls (number→NumberInput, date→date picker, enum→picker, text→text) + sort-hints; regex ops advanced-only; **op reconciles to the field type and writes back** (Apply uses the shown op) — *(R15)*
- ✅ **C3** Recursive `<GroupEditor>` nested AND/OR ("Match All/Any of:"); keep col-vs-val; **progressive disclosure** (Advanced toggle reveals groups/expr/regex) — *(R17)*
- ✅ **C4** Expression mode: per-condition textarea + ⓘ syntax help/examples + live parse-error + live match-count — verified ("⚠ unexpected end of expression" shown live) — *(R16, R17)*
- ✅ **C5** "Apply" auto-dismisses (verified); advanced (group/OR/expr) → client-side, simple flat-AND → server (`splitFilters` extended, backward-compatible wire; `conditionCount` for the badge) — *(R9)*
- ✅ Commit Phase C · suite green (227/227) · `4c2bb5a`
- ✅ **R26b** (deferred within Phase C) Filter tip runners-up via a `[H|M]?\d?:` value prefix on the `tip` field — pure `parseTipFilter`/`tipCandidateMarket`/`tipCandidateOutcome` in `filterExpr.js`, wired into `evalCond` (tip branch) + `splitFilters` (prefixed tip → client-side), UI offers contains/not-contains on tip + a prefix hint; suite 250/250; browser-verified on 07-09 (H2:O→23, M1:→21 = independent oracle) — *(R26)*

## Phase D — Betslip mobile/touch ✅
> Autonomous session 2026-07-11 (user stepped off). Scope decision: **D1–D6 core only**, presentation-only (no magic-rules engine or leg-model change). Config-collapse decision: **collapse on mobile only + summary line** (mirrors the existing Tips/Slips `togglePane`/`bodyCls` pattern; desktop keeps the full input row). Autonomous sub-decisions: D2 shrink the + to a small chip with a ≥44px invisible hit-zone (`after:-inset-2.5`), auto-hide the + only when the ACTIVE slip is full, no-slip tap auto-creates a slip seeded with that one tip (guardrail: only when zero slips exist), unify on the ascii "+" glyph; D4 outcome pill separated onto its own row + a `grid-cols-2 sm:grid-cols-4` metrics grid shared by per-slip AND footer totals (AX6 good/caution/bad pill scale); D5 add `time` (HH:MM from `start_time`) to each candidate — same-day legs show the kickoff time, off-day legs a short date ("Jul 9").
- ✅ **D1** Config inputs collapse on mobile behind a ▸ summary line (`cfgOpen` + `contents`/`hidden md:contents` — desktop unchanged); pool stays bounded (mobile `max-h-[38vh]`) — *(R5)*
- ✅ **D2** Tip ＋ shrunk to a `w-6 h-6` chip with a ≥44px `after:-inset-2.5` tap-zone; auto-hidden when the active slip is full (`slipFull`); no-slip tap auto-creates a one-tip slip (`addToActiveOrNew`, guarded to empty book); unified on ascii "+" — *(R5)*
- ✅ **D3** Removed the "adding here" badge; active slip now reads via a stronger `border-accent ring-2 ring-accent/40 bg-accent-soft/40`; the "Tap + to add to {name}" hint stays (+ a full-slip variant) — *(R25)*
- ✅ **D4** Uniform `grid-cols-2 sm:grid-cols-4` metrics grid shared by per-slip AND footer totals; outcome separated into an `OutcomePill` (won/lost/open good-caution-bad scale, AX6) — *(R10, R25)*
- ✅ **D5** Each candidate carries `time` (HH:MM from `start_time`); same-day legs show the kickoff time, off-day legs a short date (`_shortDate` → "Jul 9"); legacy stored legs without `time` degrade to nothing — *(R21)*
- ✅ **D6** Verified: candidates derive from the loaded date's `rows` and re-rank via `orderRows(chain)` — pool follows date + sort; slips persist unchanged — *(R24)*
- ✅ Commit Phase D · suite 257/257 · build clean · **browser-verified** (2026-07-11, :3001 serving the fresh dist, 464 records): full config row on desktop, "Slip is full → + New slip" hint with the + chips auto-hidden, active-slip accent border (no badge), `LOST` OutcomePill + uniform metrics grid, ✓/✗ legs — zero console errors

## Phase E — Forms & shell uniformity ✅
> Autonomous session 2026-07-11. E1 scoped pragmatically: shipped the reusable `Field` primitive and adopted it where hover-only hints hurt most (the Safe-limits form, now a 2-col grid with visible hints); broader form adoption left incremental (YAGNI — avoid a risky app-wide rewrite without browser verification).
- ✅ **E1** New `web/src/components/Field.jsx` (visible label + optional visible hint, stacked/inline); `SafeLimit` refactored onto it — hover-only `title` hints are now on-screen, fields in a clean `grid-cols-2` — *(R11)*
- ✅ **E2** Pure `web/src/reorder.js` `moveToPosition(items,key,pos)` (+ `tests/reorder.test.js`, 7 tests); ReorderList gained an optional `onReorder(keys)` that renders a commit-on-blur `PositionInput` per row — wired into Column order, Sort priority AND Providers — *(R12)*
- ✅ **E3** Footer regrouped into two uniform sections (record+day-rates | betting ledger) with a `sm:border-l` divider replacing the inline `|` — *(R10)*
- ✅ **E4** `__APP_VERSION__` vite `define` (from `pkg.version`) → tiny greyed `v1.0.1` absolutely positioned under the right toolbar icons (`pointer-events-none`, desktop only) — *(R7)*
- ✅ **E5** Settings footer "Clear cache & reload" — `window.confirm` → wipe every `oddspro.*` localStorage key → `location.reload()` — *(R8)*
- ✅ **E6** Default theme System → **Light** (`getTheme` fallback + index.html FOUC guard: unset/`light`/`dark` force `data-theme`, only `system` follows the OS) — *(R6)*
- ✅ **E7** Already satisfied by Phase C — FilterBuilder's `apply` calls `onApply(...)` then `onClose()`; Clear intentionally stays open to rebuild. Verified, no change — *(R9)*
- ✅ Commit Phase E · suite 264/264 · build clean · **browser-verified** (2026-07-11): E4 `v1.0.1` badge top-right, E3 two-section footer with divider, E5 "Clear cache & reload" in the Settings footer, E2 editable position inputs in the Providers dropdown (checkbox + boxed number + ↑/↓), E1 Safe-limits 2×2 grid with visible hints — zero console errors

## Phase F — Loading & refresh UX ✅
> Autonomous session 2026-07-11. F3 is a backend refresh-lifecycle change and could NOT be integration-verified without a live DB/server — its pure decision logic (`refreshOutcome`) and the client gate (`isDateStale`) are offline-tested, the backend passes `node --check`, and the wiring is additive (happy path unchanged: `cancelRequested` defaults false → `shouldCancel()` is always false → no behavior change). **The live cancel/resume flow needs the user's browser+DB verification before deploy.** Resume leverages pipeline idempotency (re-run skips finished steps via the fetch-once flags) rather than persisting a resume cursor — simpler and safe.
- ✅ **F1** DataTable non-silent load now blurs the stale table (`blur-[1.5px] opacity-70`) with a floating centred spinner pill overlay; silent background reloads still pass `loading=false` so they never dim — *(R18)*
- ✅ **F2** Pure `isDateStale({freshestAt,isPast,isAllDates,now,maxAgeMinutes})` in `web/src/freshness.js` (+ 6 tests) — only today/future dates go stale; App computes `freshestAt` (newest row `updated_at`) and shows a discreet dismissible nudge (per-date dismissal) with a Refresh action — *(R19)*
- ✅ **F3** Cooperative cancel: `refreshJob.cancelRequested`/`cancelled`/`last_step`, `requestCancel()`, `POST /api/refresh/cancel` (CSRF-guarded); `_step` in `runDateRefresh`/`runStartPipeline`/`lightRefresh` aborts at step boundaries; pure `refreshOutcome({error,cancelRequested})` (+ 3 tests) classifies ok/error/cancelled (cancel wins, only ok bumps version); a cancelled manual run skips the cooldown so **Resume** (re-POST, idempotent) works immediately; App shows a running-banner Cancel + a cancelled-banner Resume/Dismiss — *(R20)*
- ✅ Commit Phase F · suite 273/273 · web build clean · backend `node --check` clean. **F1/F2 browser-verified** (2026-07-11) — table renders, no console errors. **F3 NOT live-verified**: the running :3001 server holds OLD backend code (its `/api/refresh` status lacks the new `cancelled` field, and `/api/refresh/cancel` doesn't exist there yet), so cancel/resume needs a server restart + a real in-flight refresh to exercise. The frontend banners/handlers compile and the pure logic is tested; **restart `npm run serve` then cancel a live refresh before deploy.**

## Phase H — sticky Tip parity + refresh simplification + deadlock fix (2026-07-11, user-requested)
> Follow-up polish after A→F. User asks (verbatim intent): sticky Tip column didn't take the recent Tip-cell updates; the background/manual refresh flashed a raw *Insert SQL error*; move the refresh busy indicator + progress to the refresh button only; drop the "data needs a refresh" nudge (changed mind) and the progress-log banner. Design approved inline. Decisions: sticky Tip → **full parity**; Cancel/Resume → **dropped** (button-only feedback). Added ask: **sticky columns must fit content compactly** (no wasted horizontal width).
- ✅ **H1** Sticky Tip full parity — removed the `compact = col.pin` gating in `DataTable.jsx` `_cell`; the left-pinned Tip duplicate now renders identically to the real column (win-%, top-3 runners-up + their ✓/✗, colour tiers, click-popover). Pin is measured to the real column width, so it aligns. **Browser-verified** (2026-07-11): scrolled the real Tip off-screen → pinned Tip showed "🔥 U 4.5 · 72%✓ / 2. O 3.5 · 54%✗ / 3. 2 · 52%✓".
- ✅ **H2** Sticky-column compactness — pins fit content, no stretch. Measured on the dense view (scrollWidth 2646 > client): **Select 40px · Score 54px · Tip 111px = 205px total** left-pinned. The `w-full` table overflows in the real view, so columns sit at nowrap content-width (no distribution stretch); no change needed beyond parity.
- ✅ **H3** Deadlock resilience — new pure `src/db/retry-rules.js` (`isRetryableDbError`/`retryDelayMs`/`withRetry`, zero-import, offline-tested, 9 tests); wrapped the two deadlock-prone writers a manual refresh runs: `apisports.js` `_saveFixtureItems` upserts (the *observed* `teams` deadlock) + `store.js` `saveMatches` per-match odds txn (documented gap-lock; `counts` moved outside the txn so a retry can't double-count). Both idempotent → safe retry, no extra API quota.
- ✅ **H4** Refresh UX simplification (`App.jsx` + `api.js`) — removed the running/progress banner, the cancelled/Resume banner AND the stale-date nudge; dropped their state/handlers (`freshestAt`, `showStale`, `staleDismissedDate`, `cancelDismissed`, `showCancelled`, `onCancel`, `onResume`, the `isDateStale`/`cancelRefresh` imports; deleted `cancelRefresh` from `api.js`). Busy = the refresh button only (spinner + step/last-refresh in its tooltip). Backend F3 (`/api/refresh/cancel`, `requestCancel`, `refreshOutcome`) left dormant; `isDateStale` + tests kept in `freshness.js`. **Browser-verified**: idle tooltip "Refresh fixtures, results & odds — last 20:26"; zero `role=status/alert` banners in the DOM.
- ✅ **H5** Friendly UI error — the manual-job-failure path stops dumping `st.error`; shows `Refresh failed — please try again in a moment.` Full error stays in `console.error` + `logs/auto-refresh.log`.
- ✅ **H6** Verify — new `tests/retry-rules.test.js`; full `npm test` **282/282** (was 273; +9 retry); `npm run build:web` clean (66 modules); `node --check` on all touched backend files; browser (fresh dist on the running :3001): sticky Tip parity + compact widths, no refresh banners, button-only busy feedback, **zero console errors**.
- NOTE: root cause of the deadlock is a **concurrent writer in another process** (a 2nd `serve`, a CLI run, or cron) — the retry self-heals it, but only one `serve` should run (Windows npm wrappers can orphan a 2nd). H3's backend retry could NOT be exercised against a live deadlock (the already-running :3001 holds OLD backend code); it's covered by the 9 unit tests. A `serve` restart is needed before deploy for the retry to take effect live.

## Phase I — audit-extras completion + mobile popup dismiss (2026-07-11, user-requested)
> User: "tidy the stale checkboxes and the optional backlog so we can work on a clean Phase G" + "I like whats in the audit extras" + "ensure the toolbar menu dropdown auto-dismisses on mobile". Decisions: provider hues → **blue+violet**; R26d → **build now**. Cleared the WHOLE optional backlog so Phase G is the only pending phase.
- ✅ **I1 (AX4)** Provider badge hues — new `--provider-a`(blue)/`--provider-b`(violet) theme tokens (all 4 theme blocks) + `PROVIDER_STYLE` uses them. Verified both themes (light `#2563EB`/`#7C3AED`, dark brighter).
- ✅ **I2 (AX5)** `Toggle` rows `min-h-[44px]` (+ `select-none`, `shrink-0` box) — measured 44px.
- ✅ **I3 (AX7)** Betslip fixture-name tap-to-expand (`expandedNames` set toggles truncate↔`whitespace-normal`), candidate list + slip legs.
- ✅ **I4 (AX8)** Table right-edge fade (`atEnd` from scroll math; `bg-gradient-to-l from-surface`, hides at end / when not scrollable). Verified class toggles opacity-100↔opacity-0.
- ✅ **I5 (AX9)** Empty-state: magnifier icon + context message (`filterCount>0` → "No rows match your filters" + Clear-filters button; else "No correlated records"). `filterCount`/`onClearFilters` passed from App.
- ✅ **I6 (R26c)** Footer 2nd/3rd runner-up hit-rates (same-denominator gate).
- ✅ **I7 (R26d)** Betslip per-leg market switcher (`legPicks` pure helper + tests; `<select>` recomputes leg + slip metrics).
- ✅ **I8 (mobile popup dismiss)** OverflowMenu / CalendarPopover / TipPopover backdrops switched `onClick`→`onPointerDown` (+`cursor-pointer`): iOS Safari never synthesizes a click on a bare `<div>`, so tap-away failed to dismiss on mobile. `useAnchoredPanel`-backed dropdowns (MultiSelect/ReorderList) already used pointerdown. Sheet modal scrims left as-is (mostly pinned; a scrim onPointerDown would need a matching card stopPropagation).
- ✅ **I9** AX10/AX11/guardrails verified already-satisfied (mobile sheet footers reachable at 390px; `IconMenu`≠`IconFilter`; stale-grey vs blur-dim distinct; active-icon = `navBtnActive`).
- ✅ **I10** Verify — new `legPicks` tests (4); full `npm test` **286/286**; `build:web` clean; browser-verified on the running :3001 (light+dark, desktop+390px mobile): provider chips, edge-fade toggle, empty state, ≥44px rows, footer runner-up rates, per-leg market swap recompute, name-expand, mobile popup dismiss — **zero console errors**.

## Phase J — popup dismiss, footer, betslip guard, help/copy polish (2026-07-11, user-requested)
> Rapid-fire user feedback after Phase I. All browser-verified on the running :3001 (light+dark, desktop+390px), suite 286/286, zero console errors.
- ✅ **J1** Header popups (OverflowMenu + CalendarPopover) now dismiss on tap-away **anywhere** (incl. footer/table) — root cause was the nav's `backdrop-filter`, which becomes the containing block for a `fixed` descendant, so their `fixed inset-0` backdrop only covered the header strip and a tap on the page never reached it. New `web/src/useOutsideDismiss.js` (document `pointerdown`, no stacking/containing-block dependency; matches `useAnchoredPanel`); backdrops removed; wrapper refs in App. Verified: real footer-coordinate pointerdown closes the overflow menu; outside-table tap closes the calendar. (My earlier onClick→onPointerDown-on-backdrop fix in Phase I didn't help — the backdrop wasn't where the tap was.)
- ✅ **J2** Magic-sort sheet auto-dismisses on selecting a strategy (`onToggle` also `setShowMagic(false)`) — quick single-sort; reopen to add more. Verified (sheet closed + pill applied).
- ✅ **J3** Score column only sticky when the day HAS scores (`scoreHasData` gates `PIN_KEYS`; `pins` filter also requires PIN_KEYS membership so a stale pinState can't pin an empty column). Verified: future date (no scores) pins only [Select, Tip].
- ✅ **J4** Footer status bar rebuilt compact + uniform — one flex row, gap-spaced (no ragged `·` wrap), integer percents only (`_ratePct`; full fractions in tooltips), `tabular-nums`, `text-[11px]`, tighter padding. `497 · 🔥 75% · Tips 73% · 2·56% 3·66% · 🛡 0 | 💰 159 · Σ231.78 · ≈23,178 · 43✓ 16✗ · +334`. Playground totals already grid-uniform (D4) — left as-is.
- ✅ **J5** Betslip: enforced no-add when the active slip is full — `addToActiveOrNew` returns early on a full active slip; a full slip is no longer a drop target (no accept-border, drop ignored). (Couldn't reproduce a removal in current code; the + is already hidden — this hardens every path.)
- ✅ **J6** Help window de-discloses the prediction methodology (user: "keep it our internal secret") — dropped "backtested confidence model" / "backtested strategies"; Magic now reads "re-orders tips so the strongest come first". (NOTE: the TipPopover still explains the blend/signals/weights to laymen — flagged to the user as a separate call.)
- ✅ **J7** Removed all em dashes from the web UI (81→0 across `web/src`, U+2014→`-` via a precise perl pass; none were in comparisons/regex — placeholders + text separators only). Suite unaffected (the one test em dash was a comment).
- ✅ **J8** (follow-up to J6) TipPopover rebuilt as a lean **bet-decision card** — pick + odds + plain label, a confidence rating (verdict only), 🛡 Safe / 🔥 high-scoring flags, "Other options" (alternatives + odds), settled result. The **methodology is now a mystery**: the blend components + weights, evidence samples, the over-2.5 gate audit and the AI's findings are **preserved behind `SHOW_INTERNALS=false`** (flip it / thread a `premium` prop when session-based premium tiers land). Also softened the DataTable 🔥-badge tooltip (was the signal audit / AI reason / internal score) and the Tip-cell tooltip (was "market+stats confidence" + the AI-veto reason); dropped the now-unused `SIGNAL_LABEL`/`signalValue` import there. Browser-verified: popover + both tooltips show ZERO methodology terms (blend/weights/samples/gate/AI), bundle shrank ~4 KB. **NOTE for premium phase:** the raw `tip_breakdown`/`hot_signals`/AI-review fields are STILL in the records API payload (visible in devtools/network) — gate them server-side when premium lands for true secrecy.

## Phase G — Docs, standards, DB, deploy *(after "we are ready for deployment")*
- ⬜ **G1** Standards + memory updates (project/global CLAUDE.md, memory-bank, auto-memory) — *(M1)*
- ⬜ **G2** DEPLOYMENT.md v1.0.1 additions — *(M2)*
- ⬜ **G3** In-script `knex.migrate.latest()` on boot (schema-only, env-guarded) — *(M3)*
- ⬜ **G4** Final cross-check vs original drafts + implementation report — *(M5, M6)*
- ⬜ **G5** Merge dev→main (suite green → push → PR → merge → keep dev) — *(M7)*

---

## R26 — Tip runners-up at a glance (added mid-session 2026-07-11)
> `bestTip` already persists runners-up in `tip_breakdown`; each runner-up ✓/✗ is settle-able client-side via `tipHit(market, hg, ag)` on the final score.
- ✅ **R26a** Tip cell shows top-3 picks (chosen + 2 runners-up) stacked, each with hit/miss tick; chosen bolder — done in Phase B (line 52) + the sticky duplicate reached parity in Phase H
- ✅ **R26b** Filter tip runners-up via a value prefix on the `tip` column. Grammar: `[H|M]?\d?:value`.
  - `\d` = candidate index: `1:O 2.5` (chosen/default), `2:O 2.0` (2nd), `3:O 2.5` (3rd); bare value = 1st.
  - `H|M` = settled-outcome filter (added 2026-07-11): `H1:O 2.5` = 1st-pick O 2.5 that HIT, `M1:O 2.5` = that MISSED; no `H`/`M` = all outcomes. Combines with the index (`H2:...`, `M:...`).
  - Strip the prefix in value preprocessing before the op runs; resolve the Nth candidate market from `tip_breakdown` and grade hit/miss via `tipHit` on the final score — *C*
  - DONE (2026-07-11): pure `parseTipFilter`/`tipCandidateMarket`/`tipCandidateOutcome` (filterExpr.js), `evalCond` tip-prefix branch (AND-combines the outcome gate with the market predicate), prefixed-tip conditions forced client-side in `splitFilters`, FilterBuilder offers contains/not-contains on tip + prefix hint. Tests: `tests/tip-filter.test.js` (14) + splitFilters routing (1). Colon-required prefix = backward compatible (plain markets have no colon). Uniform tipHit-from-score grading = display parity with the tip-cell runners-up ticks.
- ✅ **R26c** Footer 2nd + 3rd runner-up hit-rates — Phase I. Graded from the final score via `tipHit`, over the SAME settled fixtures as the Tips line (comparable denominator). Verified: `Tips 43/59 · 2nd 33/59 · 3rd 39/59` (chosen beats both runners-up).
- ✅ **R26d** Betslip per-leg tip-market select (chosen / 2nd / 3rd) — Phase I. Pure `legPicks(row, cal)` in magic-rules (re-estimates each option's prob; 4 tests); leg `<select>` recomputes price/prob/outcome and the slip's odds/payout/survival/EV. Verified: switching leg 1 to the 2nd pick moved odds 2.82→3.07, EV +2.1%→+16.0%.
- ✅ **R26e** (user-requested 2026-07-11) **Tip confidence %** filter field — the chosen tip's overall confidence on a 0-100 **integer** scale (`sortValues.tip_confidence` = `round(conf×100)`, matches the displayed %). Client-only derived field force-listed in the Betting group (like `score`), NumberInput clamped `{min:0,max:100,int}`. Suite 253/253; browser-verified on 07-09 (`gte 80`→18, `eq 80`→4 = oracle; 150 clamps to 100) — *C*

## R27 — "No" column + full column freedom (added mid-session 2026-07-11)
- ✅ **R27a** Synthetic **"No"** column = live 1-based position in the displayed order; shown/toggle/sortable (`sortValues.no` reads a stamped `_no` load-order anchor) — verified — *B/E*
- ✅ **R27b** ALL base columns toggle-able via a new **"Table columns"** MultiSelect (Select/No + Start/Fixture/Provider/Score/Goals/Tip/Status); persists `oddspro.cols.base` (null = all) — verified (hid Provider) — *E*
- ✅ **R27c** No is sortable (shift-click isolates → reorders by load order; cycles desc→asc→off); base columns already sortable via header — verified — *B*
- ✅ **R27d** "No" as a filter field — DONE (2026-07-11): moved the load-order anchor UP to the App (`noAnchor` memoized on `result`, so it's stable across client-filter/selection edits and only renumbers when a new fetch lands), stamping `_no` on `numberedData` UPSTREAM of `applyClientFilters` — so `no` filters on the same load-order value it sorts by (non-circular). `DataTable`'s own anchor removed (single source; it now reads `r._no`). `no` force-listed in FilterBuilder's Match-info group (number field, NumberInput `{min:1,int}`), label via `EXTRA_BASE_LABELS`, `sortValues.no` comment updated; routes client-side automatically (synthetic key ∉ server catalog). Browser-verified (`no ≤ 5` → 5 rows / No 1–5). Suite 255/255 — *C*
- ✅ **R27e** "**Pin position numbers**" toggle (Settings, default off): freezes No to each row's load-order anchor per date so re-sorting doesn't renumber — verified (No showed 425,424,… under a No-desc sort) — *B/E*

## R28 — "Select" checkbox column + row-selection actions (added mid-session 2026-07-11)
> Shares the synthetic-column + visibility infrastructure with R27; build alongside it. Row selection state is a client concern (persisted per display-date), never sent to the server.
- ✅ **R28a** Synthetic **"Select"** column = ALWAYS leftmost (fixed-width left pin); header = a check/uncheck-all checkbox (indeterminate when partial), title "Select / deselect all shown"; per-row checkbox. Toggle-able via the "Table columns" MultiSelect (hiding it clears the current date's selection); NOT sortable — verified — *B/E*
- ✅ **R28b** Selection persisted per display date (`oddspro.select.d.<date>`), keyed by **match_id** (stable identity), default all-unselected; survives reload (verified `[5289]` persisted) AND filtering (the Set is never pruned by the view — `stampSelection` re-stamps from it, so filtered-out rows return still-checked). **"Clear all selections"** wipes every `.d.` key — *B/E*
- ✅ **R28c** Settings **"With selected (N)"** block. **"Hide selection"** toggle removes checked rows upstream at `visibleData`, so table + filter options + day calcs + betslip pool all exclude them at once; built slips keep their own legs (self-contained) — verified (423/425) — *E/D*
- ✅ **R28d** Filters: the **Select** field (type `bool`) renders a checkbox value control ("selected / not selected", stores 1/0); `$row['select']` works in expressions — verified — *C*
- ✅ **R28e** "With selected → **Export CSV (N)**": downloads the checked rows as a full-record CSV (pure `web/src/exportCsv.js` `toCsvString`/`buildRecordCsv`, 6 tests) — every field incl. columns hidden from the table (ID/Updated/Locked/team names/canonical) + every market + every post-match stat (76 cols verified), RFC-4180 escaping, UTF-8 BOM for Excel; acts on the selection (ignores Hide-selection). Verified in-browser (BOM bytes EF BB BF, 2 rows) — *E*
- ✅ **R28f** (user-requested 2026-07-11) **"Keep selection"** toggle — the inverse of Hide selection: hide every UNCHECKED row so only the checked ones remain across the table, filter options, day calcs and the betslip pool. Pure `applySelectionKeep(rows, keep)` in `filterValues.js` (2 tests), applied over `applySelectionHide` in `visibleData`; the two are **mutually exclusive** (enabling one clears the other in the App setters, so the view can't be emptied by both narrowing at once). Browser-verified (Keep → 2/425; enabling Hide flipped Keep off → 423/425). — *E/D*

## R31 — Displayed-records footer betting ledger (user-requested 2026-07-11)
- ✅ **R31a** ENHANCE the existing footer (no new panel) with a betting-ledger summary over the rows CURRENTLY SHOWN — auto-recomputes as the table re-renders (filters/sort/hides/toggles). Each displayed pick = one flat betslip-stake bet, deduped to one per canonical fixture (`api_id`). Each requested quantity is its OWN inline segment (wraps on narrow screens): **💰 N picks · odds Σ X · value ≈V · W won · L lost · P/L ±Z** (won green, lost red, P/L coloured). Pure `displayedSummary(rows, stake)` in `filterValues.js` (2 tests) — picks/totalOdds/value(=stake×Σodds, the all-won ceiling)/won/lost/staked/returned/profit(=returned−staked, settled-only, same convention as the betslip's `slipTotals`). Stake mirrors the **betslip playground's** persisted config (`oddspro.betslips`.config.stake, re-read on modal close so the two never diverge; default 100). Replaced the first (mis-read) "Shown —" hit-rate group. Suite 257/257; browser-verified (recomputes on filter; rescales with stake; segments split out). — *E/footer*

## R29 — Tip cell clickable affordance (added mid-session 2026-07-11)
- ✅ **R29a** Tip cell value hints clickability (dotted hover-underline on the pick name + skip marker) — committed `3aa1253` — *B*

## R30 — Multi-line cell polish (added mid-session 2026-07-11)
> The Tip cell now stacks up to 3 lines (runners-up); use that vertical rhythm to make neighbouring cells fit content better.
- ✅ **R30a** Start column → two lines: tiny greyed ID on top, `HH:MM` below — verified — *B*
- ✅ **R30b** Sweep other cells for tasteful use of the taller row — DONE (2026-07-11): the honest outcome of the sweep is that most cells read better single-line (they vertically-center cleanly against the tall Tip/Start cells), so the one tasteful add is the **Status cell** — in-play games (`LIVE_MINUTE` + `elapsed`) now show a second muted line with the live minute (`1H` / `22'`), mirroring the Start cell's id-over-time rhythm and surfacing the minute without hovering (touch-friendly); static statuses (NS/FT/HT/PST) stay single-line. Browser-verified against live fixtures (`1H22'`, `2H53'`). — *B*

## Cross-cutting
- ⬜ **M4** Browser-test protocol: work-area − 90px taskbar, reuse one tab, close page/context on cleanup (applies to every browser verification)

## Notes / findings log
- _(append insights here as work proceeds)_
- **Autonomous session 2026-07-11 (Phases D→F)** The user stepped off and delegated completion of the remaining non-deploy-gated phases. Verification: `npm test` (grew 257→273), `npm run build:web` (clean), `node --check` on touched backend files, bundle-grep for the E4 version define, AND a live browser smoke-test against the already-running :3001 (which statically serves the freshly-built dist over real data — 464 records; the API server itself was NOT restarted, so it still runs OLD backend code). D/E/F1/F2 were browser-verified with ZERO console errors. **F3's live cancel/resume was NOT exercised** — the old server lacks the new endpoint/fields; it needs a `serve` restart + a real in-flight refresh. Phase G stays gated on the user's explicit "we are ready for deployment". Nothing was merged to main or deployed; all work is on `dev` for review. New pure helpers + tests: `web/src/reorder.js` `moveToPosition` (7), `web/src/freshness.js` `isDateStale` (6), `src/db/auto-rules.js` `refreshOutcome` (3). New components: `Field.jsx`. F3 wiring is additive — the happy path is unchanged because `cancelRequested` defaults false.
- **R27a follow-up (2026-07-11)** "No" defaults to the first column AFTER the pinned Select checkbox even when a **legacy** `oddspro.cols.order` (saved before the column existed) is present — `applyOrder` would otherwise append the unknown key at the end. DataTable now front-loads `no` unless the saved order EXPLICITLY lists it (an intentional reorder is respected). Browser-verified: legacy order → `[☑, No, Start…]`; explicit `…,no,…` order → honoured in place.
- **Refetch-loop hardening (2026-07-11)** Investigated a suspected re-render/refetch loop on zero-record filters. Root cause: **none** — client-only filters never refetch, a server filter refetches exactly once, and idle-at-zero is fully quiescent (0 DOM mutations / 0 fetches / 0 errors, browser-measured). Added **defense-in-depth** anyway: the records `useEffect` now serialises its query into `lastQueryRef` and (a) skips a re-run whose query is byte-identical — neutralising the documented "unstable-reference dep → refetch every render" trap — and (b) applies a response only while its key is still current, so a superseded (stale) response is ignored WITHOUT the old per-run cleanup flag. NOTE: a first naive early-return attempt regressed the initial load to 0 records (the previous run's cleanup had already staled the in-flight fetch) — the `lastQueryRef`-as-marker rewrite is the correct form. Suite 257/257; browser-verified across load/date-nav/filter/refresh.
