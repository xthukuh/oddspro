# oddspro — Implementation Plan & Progress

Source spec: `C:\Users\User\.claude\plans\plan-the-oddspro-project-gleaming-truffle.md`
Goal: MySQL data warehouse for bookmaker odds (BetPawa, Betika) + api-sports.io football stats, with cross-source match linking. Analysis/prediction/dashboards out of scope.

## Status legend
- [ ] pending · [~] in progress · [x] completed (implemented AND verified)

## Phase 1 — Foundation
- [x] Add dependencies: knex, mysql2, dotenv, zod
- [x] `src/config.js` — dotenv + zod env validation (DB_*, X_APISPORTS_*, optional overrides)
- [x] `src/utils.js` — extract shared `_pint`/`_date`/`_dtime`/`_batch` from both scrapers; fix `warn` → `console.warn` bug in `_batch`
- [x] Rewire `src/betpawa.js` / `src/betika.js` to import from `src/utils.js` (no behavior change; syntax + import smoke-tested)
- [x] `knexfile.js` + `src/db/connection.js` (knex singleton)
- [x] Migration `src/db/migrations/*_init_schema.js` — full warehouse schema (11 tables, utf8mb4, timestamps, FKs) — written, NOT yet run
- [x] `npm run migrate` script; `.env.example`
- [x] Verify: migration runs clean on fresh `oddspro` DB (14 tables incl. knex bookkeeping; unique keys + FK rules introspected and confirmed)
- [x] Commit `feat: warehouse foundation (db layer, schema, shared utils)`

## Phase 2 — Odds persistence
- [x] `src/db/store.js` — `saveMatches()`: upsert `matches` by (provider, provider_match_id); delete+insert `odds_markets`; skip completed matches
- [x] Rewire `betpawa`/`betika` CLI actions to persist to DB (stop writing x-*.json; existing files untouched)
- [x] BONUS: fixed pre-existing `_batch` race - resolved before in-flight tasks drained (caused "Found 37" vs 47 saved; old JSON dumps could contain holes)
- [x] Verify: betpawa 47 matches/11073 markets; betika 164 matches/26683 markets; betpawa re-run → 0 inserted/47 updated, market count identical (clean replace)
- [x] Commit

## Phase 3 — api-sports fixtures & results
- [x] `src/apisports.js` — client (x-apisports-key header, zod validation, quota guard on x-ratelimit-requests-remaining, pagination)
- [x] `fixtures [date]` action — upsert leagues/teams/fixtures (timezone=Africa/Nairobi)
- [x] `results` action — refresh past-kickoff unfinished fixtures (ids batched 20/req); settle scores into linked matches; set `matches.completed_at` (linked terminal OR start_time > 4h past)
- [x] MySQL session time_zone pinned to +03:00 (knex pool afterCreate) so NOW() aligns with stored EAT wall-clock datetimes
- [x] Verify: 111 fixtures / 30 leagues / 220 teams upserted; results refreshed 11 in-play fixtures in 1 request; statuses NS/FT/HT/2H/PEN/CANC/INT present; quota remaining 149,998 (high-volume plan). Full settle check pends Phase 4 links + time passing.
- NOTE: zod caught `league.round` = null on live data (schema fixed) — keep schemas tolerant on nullable fields
- [x] Commit

## Phase 4 — Match linking (revised per 2026-07-02 README: fixtures = canonical base)
- [x] Name normalizer + fuzzy similarity scorer with `LINK_MIN_CONFIDENCE` threshold (default 0.85)
- [x] `link` action, provider order **betpawa → betika** (betika also scores against linked betpawa records; kickoff ±30min window; competition/league similarity included)
- [x] `team_aliases` + `league_aliases` learning + auto-link after odds/fixtures ingestion
- [x] Scorer iteration after live near-miss review: best-of(bigram dice, token-set dice, 0.9×overlap coefficient, initialism match); reserve markers II≡B≡2; club-type prefixes (JK/NK/US/AS/CS/CR/RS...) as noise; competition = corroborating bonus (+0.1×sim) never a veto; 0.05 runner-up margin guard
- [x] Verify: betpawa 26/47 (55%), betika 29/164 (18% — most unmatched lack an API-Football fixture that day); 14/14 spot-checks correct incl. initialism/word-flip/reserve cases; 55 links → 31 distinct fixtures (cross-bookmaker convergence); 110 team + 29 league aliases learned. Alias fast-path exercise pends next day's fresh matches.
- NOTE: tuning knobs for user: `LINK_MIN_CONFIDENCE` (.env), weights/margin in `src/link.js` `_confidence()`. Alias cache growing as designed: 1,475 team + 132 league aliases by 2026-07-03
- [x] Commit

## Phase 5 — Deep stats & standings
- [x] `stats` action — statistics + lineups + events per final correlated fixture (fetch-once flags; empty responses only flagged 48h post-kickoff; serial DB writes)
- [x] `standings` action — per league/season on correlated fixtures; delete+replace rows; teams upserted for FK safety
- [x] Deadlock fix: `_batch` concurrency 1 for delete+insert transaction workloads (parallel workers deadlocked on InnoDB index gap locks)
- [x] Verify: standings 204 rows / 15 league-seasons, re-run idempotent (204 again), 3 table-less comps skipped; stats action correctly targets 0 fixtures at 6:45 AM (nothing final+correlated yet) with 0 API calls
- [x] Full stats-path live check VERIFIED 2026-07-03: `fixture_statistics` 252 rows / 12 fixtures (9 stat types each), `fixture_lineups` 7 rows / 4 fixtures, `fixture_events` 387 rows / 35 fixtures — rows land once leagues publish; fetch-once flags match (12/4/35)
- [x] Update `CLAUDE.md` (rewritten: pipeline architecture, key invariants, all 7 actions, env names)
- [x] Commit

## Phase 6 — Visualization (added per 2026-07-02 README revision)
- [x] Market mapping layer `src/markets.js`: provider market type/name → canonical columns (1, X, 2, 1X, X2, 12, U/O 0.5–6.5; defaults 1.5–4.5). Match on `type_name` never `type_id` (betika reuses id 19 across team-total markets). Single registry drives both JS pivot and SQL sort/filter conditions.
- [x] Shared read layer `src/db/records.js` — `queryRecords()` (paginate/multi-sort/filter; market columns sortable via LEFT JOIN pivot subqueries) + `columnCatalog()` (dynamic STATS list from fixture_statistics)
- [x] `export [date]` action (`src/export.js`) — temp CSV (tmp/, gitignored, BOM for Excel) with README column spec, correlated records only
- [x] API server `src/server.js` (:3001, `npm run serve`): GET /api/records (paginated + multi-sort + filter ops eq/ne/gt/gte/lt/lte/like), GET /api/columns; serves web/dist; graceful shutdown closes knex pool
- [x] `web/` React 19 + Vite 6 + Tailwind 4 datatable (`npm run build:web`; dev :5173 proxies /api → :3001); settings modal (multi-select market + STATS columns, defaults pre-selected, localStorage-persisted); filter query builder; multi-sort headers (shift-click chains)
- [x] API-Football extras: pre-match standings rank/form per team + H2H summary (W-D-L, home perspective) derived from local warehouse (no extra API hits); post-match fixture_statistics as dynamic toggleable columns
- [x] BONUS: fixed pre-existing `_date()` bug (src/utils.js) — valid `Date` instances fell through to `new Date()`; first hit by export since mysql2 returns DATETIME as Date objects
- [x] Score/goals only surfaced once fixture status is final (results-are-canonical; BetPawa reports 0-0 pre-match)
- [x] Verify: export 55 correlated records with odds+rank/form columns; API multi-sort on `O 2.5` + filters (`1 lte 1.5 AND provider eq betika` → 7) + 400 on bad keys; browser-verified datatable/settings/filters/multi-sort via Playwright, 0 console errors
- [x] Commit

## Phase 7 — Default `npm run start` pipeline (added 2026-07-02)
- [x] `src/pipeline.js` — `runStartPipeline(days)`: full sweep today..+N days (default 3), ordered for fewest server hits: fixtures per date → results → betpawa/betika odds per date → link once → stats → standings; `[start k/7]` step banners
- [x] `src/index.js` — no action / `start [days]` / bare number (`npm run start -- 5`) dispatches the pipeline
- [x] `src/db/store.js` — `completedMatchIds(provider, from)`: exclusion set so scrapers skip per-game detail requests for completed matches (saveMatches would discard them anyway); wired into pipeline AND single betpawa/betika actions
- [x] `src/utils.js` `_progress()` helper — periodic `n/total` logs in the scraper detail batches and the deep-stats batch
- [x] Fewer-hits ordering: date-scoped fixtures fetch refreshes today's statuses first (shrinks results' per-id refresh set); results completes matches BEFORE odds scrapes; link runs once instead of 12 auto-link passes
- [x] BUGFIX (caught in live run 1): `Number(null) === 0` made the default sweep 1 date instead of 4 — switched to `parseInt` (null/undefined → NaN)
- [x] BUGFIX (caught in live run 2): standings rows with `team.id = null` (TBD playoff/bracket placeholder slots) crashed zod validation at 60/71 leagues — such rows are now skipped pre-parse (no FK target to store anyway)
- [x] Verify: live `npm run start` end-to-end — 4-date sweep (fixtures 111/203/570/276), results settled 6, betpawa 37+126+161+104 games, betika 168+138+244+108, single link pass (betpawa 268 + betika 319 fuzzy-linked), stats fetch-once, standings 71 league/seasons → 1116 rows; quota-guarded throughout (~149.8k remaining)
- [x] Update `CLAUDE.md` (commands + architecture entry for `src/pipeline.js`)
- [x] Commit

## Phase 8 — Focused-date refresh button (added 2026-07-02)
- [x] `src/pipeline.js` `runDateRefresh(date, onStep)` — single-date subset: fixtures → results (skipped for future dates) → betpawa/betika odds (completed-exclusion) → link once → deep stats (skipped for future dates); standings stays owned by the full sweep; `onStep` narrates progress
- [x] `src/server.js` — `POST /api/refresh?date=YYYY-MM-DD` starts a single-slot background job (400 bad date, 409 + in-flight job when busy — parallel refreshes would deadlock on delete+insert gap locks); `GET /api/refresh` poll endpoint
- [x] `web/src/api.js` — `startRefresh()` (409 resolves to the in-flight job), `fetchRefreshStatus()`
- [x] `web/src/App.jsx` — Refresh button beside the date picker: disabled without a focused date, amber + live step label while running, 2s polling, table reload on completion, picks up an in-flight refresh on page load
- [x] Verify: endpoints via curl (idle/400/202/409); live browser flow via Playwright — click → "Refreshing 2026-07-02 — betika odds…" → idle + table reload in ~45s, 0 console errors; empty date disables the button; today-refresh cost only 9 betpawa + 127 betika detail hits (completed games pre-excluded), settled 14, stats for 8 fixtures
- [x] NOTE: stale pre-change `node src/server.js` (PID 19088) held :3001 and 404'd the new endpoints — killed and restarted with new code; server restart required after pulling these changes
- [x] Commit
- [x] Security hardening (post-review finding: CSRF/unauthenticated state-changing endpoint): `POST /api/refresh` requires `X-Requested-With` header (custom headers force a CORS preflight the server never approves — kills cross-site POSTs); server binds `API_HOST` (default `127.0.0.1`, LAN exposure now opt-in). Verified: 403 without header, 400 with header+bad date (no scrape burned), loopback-only bind via netstat, button flow re-verified in browser (31s refresh, 0 console errors)

## Phase 9 — Freshness, stale odds & compact datatable (added 2026-07-02)
- [x] Test harness FIRST (safeguard before behavior changes): `npm test` = `node --test "tests/*.test.js"` — `tests/markets.test.js` (canonical mapping incl. type_name-never-type_id invariant + whereMarket SQL via disconnected knex builder), `tests/snapshots.test.js` (zod contract of the standardized game record over the frozen `x-*-output.xx.json` scraper outputs; legacy null holes filtered — live `_batch` rejects on error), `tests/diff-odds.test.js` (all stale-diff scenarios)
- [x] Migration `20260702000002_odds_markets_stale_flag.js` — `odds_markets.is_stale` (default false; existing rows read fresh, no backfill)
- [x] `src/db/odds-diff.js` (pure, zero imports) — `oddsIdentity` (type_name/name/handicap, numeric handicap normalization: mysql2 DECIMAL strings vs snapshot numbers, NUL delimiter) + `diffOddsRows` → `{staleIds, deleteIds}`
- [x] `src/db/store.js` — diff-based odds refresh replaces blanket delete+insert (vanished markets kept flagged stale, re-listed markets revive); explicit `updated_at: db.fn.now()` on the matches UPDATE (ON UPDATE CURRENT_TIMESTAMP skips no-op updates). Verified live: 2 consecutive betika scrapes, row totals stable (no identity-mismatch duplication), only genuine churn flagged (14→104 stale)
- [x] `src/db/records.js` — rows gain `updated_at`, `markets_stale` (fresh-shadowed keys stripped), `available` (`!TERMINAL_STATUSES(status) && !completed_at && freshCount > 0`; TERMINAL = RESULT + CANC/ABD); market sort/filter pivots exclude stale; `status` moved out of STAT_COLUMNS
- [x] `src/export.js` — `status` column after `goals` (CSV parity with the UI)
- [x] `web/` — Status base column right of Goals; rainbow row tints cycled per `api_id` (same fixture across providers shares a tint); row `title` freshness tooltip; stale prices greyed slate-400 + "No longer offered"; unavailable matches unlinked ("Betting unavailable") with per-provider Settings re-enable toggle (`oddspro.links.unavailable`, default off — betpawa serves concluded pages ~6h); persisted column keys sanitized against the catalog; compact `text-xs` + tighter padding; self-hosted Inter Variable (`@fontsource-variable/inter`, Tailwind `@theme --font-sans`); navbar Date label dropped
- [x] Verify: `npm test` 14/14; migration on populated DB (274k rows fresh); live double-scrape store round-trip; API fields + market-sort via curl; CSV header; full browser pass via Playwright (columns, tints, tooltips, stale cell, unlink + toggle, settings sections, 0 console errors); temp stale flags reverted
- [x] NOTE: stale pre-change `node src/server.js` (PID 17244) held :3001 — killed and restarted with new code (recurring: restart server after backend changes)
- [x] Commits: `test:` harness → `feat:` stale retention (store) → `feat:` read layer → `feat:` frontend → `docs:` this update

## Phase 10 — Historical pre-match snapshots + rolling-goals columns (added 2026-07-02)
- [x] TDD first: `tests/prematch-calc.test.js` (11 tests, offline) written and watched fail before the module existed — h2hSummary parity (orientation both venues, strict kickoff cutoff, null-score skip), computePrematch (window caps, opponent exclusion, venue-oriented gf/ga, h2h_count beyond window, empty history), formatGoals
- [x] `src/db/prematch-calc.js` (pure, zero imports) — `h2hSummary` moved verbatim from records.js `_h2hSummary`; `computePrematch` (pair-level H2H window + per-side vs-others window); `formatGoals` → `"8/5 (2.6)"`
- [x] Migration `20260702000003_prematch.js` — `fixtures.history_fetched_at` (fetch-once flag) + `fixture_prematch` table (typed columns keyed by fixture_id, FK CASCADE)
- [x] Config: `PREMATCH_TEAM_WINDOW` / `PREMATCH_H2H_WINDOW` (default 5/5) in `src/config.js` + `.env.example`
- [x] `fetchApisportsHistory()` (apisports.js) — per upcoming correlated fixture: 2× `/fixtures?team=&last=` + 1× `/fixtures/headtohead` (no `last` → true all-time meeting count); items filtered to FINAL_STATUSES pre-save (future h2h meetings must not leak into the results refresh set); saved via `_saveFixtureItems` upserts (never deletes); per-run team dedupe Set; serial batch
- [x] `src/prematch.js` `updatePrematchSnapshots()` — upsert `fixture_prematch` for upcoming correlated fixtures; `kickoff > NOW()` selection IS the freeze; single-statement chunked `onConflict().merge()` (no delete+insert)
- [x] Pipeline: `runStartPipeline` steps 8 (history) + 9 (prematch) after standings (snapshot needs local history + fresh rank/form); `runDateRefresh` gains both for today/future dates; CLI actions `history` / `prematch`
- [x] Read layer (records.js): snapshot-preferred merge (presence of row wins wholesale — null snapshot rank ≠ fall back to live standings); live derivation fallback for pre-feature fixtures; new STAT_COLUMNS `h2h_count`, `home/away_goals_h2h`, `home/away_goals_oth` (display-only, snapshot-only); zero frontend changes (catalog-driven)
- [x] Verify: `npm test` 25/25; migration on populated DB; live `history` run — 332 fixtures, 9,417 historical fixtures backfilled (~1,150 requests, quota 148,846 left); `prematch` — 332 snapshots, re-run idempotent; spot-check (332/332 full 5-game windows, 295 with H2H, e.g. Trans Narva–Levadia 65 meetings "5W-14D-46L"); read layer serves compact strings per provider pair; 0 snapshots on past fixtures
- [x] Live freeze check VERIFIED 2026-07-03: 12 concluded fixtures hold snapshots, 0 written after kickoff, 3 already diverge from the moved live standings (e.g. fixture 1520753: frozen home rank 13 / form DLWWW vs live rank 9 / WDLWW) — the freeze holds while the world moves
- [x] Docs: CLAUDE.md (commands, architecture, invariants) + this checklist
- [x] Commit

## Post-phase-10 touches
- [x] 2026-07-03 user commit `602ed3c`: web header date navigation — `[OP]` branding, Today / ‹ prev / next › buttons, date picker bounds (min `2026-07-02`, max +7 days), noon-anchored date math (avoids UTC day-shift), `showPicker()` on focus/click, cursor-pointer polish
- [x] 2026-07-03 retrospective: remaining live checks closed with read-only DB evidence (stats path, snapshot freeze — see §5/§10); README rewritten from spec-draft to accurate project README; memory-bank goals synced to phases 1–10. Warehouse health at check time: 9,704 fixtures, 2,009 bookmaker matches (betpawa 345/510 linked, betika 400/1499), 384 frozen snapshots, 389,500 odds rows (993 stale)
- [~] Alias fast-path live observation, refined 2026-07-03 after a fresh sweep showed `0 via alias`: forensic check (matches linked today vs aliases created before today) proved all 91 of the day's new links were fuzzy because 85/91 involved never-seen team names — teams recur weekly, not daily, so a 2-day-old cache can't hit yet. Mechanism verified correct (raw-name exact Map lookup precedes fuzzy scoring in `src/link.js`; 193 aliases learned today, cache ~1,668 team + 132 league). Expect first "via alias" counts ~2026-07-08+ when 07-02's teams play again. The 142/543 "examined, unmatched" leftovers are chronic sub-threshold near-misses (0.50–0.85) with no fixture or too-different names — permanent residents until threshold tuning

## Phase 11 — Automation & prediction scoping (added 2026-07-03)
- [x] Windows Task Scheduler task `oddspro-pipeline` → `scripts/pipeline-task.cmd` (cd to repo, `npm run start`, append `logs/pipeline.log`), daily 08:00. Smoke-tested via `schtasks /run`: all 9 steps, exit 0, 0 errors, 227k+ odds rows saved, 19 newly-listed betika matches picked up
- [x] `docs/prediction-scoping.md` — prediction-phase scoping draft: warehouse offerings (frozen snapshots = leak-free features, closing odds = baseline), two dataset strategies, 7 open questions. NOT approved for implementation — awaits brainstorm with user

## Phase 12 — Over 2.5 hot picks 🔥 (rule-based, added 2026-07-03)
- [x] Design: logical deduction only (no ML) — strict AND concurrence gates over leak-free history aggregates + BetPawa vig-removed implied probability + API-Football `/predictions` boost/veto + optional OpenRouter AI adjudicator (env-gated, fail-open). Spec: `C:\Users\User\.claude\plans\` plan file; supersedes the Python/XGBoost direction in `docs/prediction-scoping.md`
- [x] Migration `20260703000001_hotpicks.js` — `fixtures.predictions_fetched_at` (fetch-once flag) + `fixture_api_predictions` (API evidence, raw json kept) + `fixture_predictions` (pick ledger: hot/score/signals/prices/ai_verdict/outcome, PK fixture_id)
- [x] `src/db/goals-rules.js` (pure, zero imports) — `impliedProbability` (two-way devig), `teamGoalsAggregates`/`h2hGoalsAggregates` (kickoff-cutoff, vs-others semantics), `apiPredictionSignal` (signed under_over line → support/contradict/neutral), `scoreOver25` (strict AND gates + composite confidence score + signals audit trail)
- [x] TDD: `tests/goals-rules.test.js` — 15 tests (devig, cutoffs, windows, each gate independently, H2H veto thinness, API neutrality, requireMarket backtest mode, boundary values). `npm test` 40/40
- [x] Backtest `scripts/backtest-hotpicks.js` — 10,678 finished fixtures replayed with kickoff cutoffs, 100-combo grid; baseline over-2.5 rate 54.3%, shipped defaults (window 7, over-rate ≥0.6, avg-total ≥3.2) = 73.2% stats-only precision (358 flagged); market/API gates only tighten go-forward
- [x] `fetchApisportsPredictions()` (apisports.js) — fetch-once `/predictions` per upcoming correlated fixture; live run 409/409 saved, rerun 0 targets (idempotent)
- [x] `src/hotpicks.js` `updateHotPicks()` — settle (canonical FT → hit/miss, owned by settle pass) → evaluate ALL upcoming correlated snapshot-backed fixtures (non-hot rows kept as calibration ledger) → AI adjudication (verdict reuse when score unchanged = no re-billing; veto flips hot, never promotes; error keeps rule verdict) → chunked onConflict merge upsert; `kickoff > NOW()` selection IS the freeze. `hotpicksSummary()` for the web chip
- [x] AI adjudicator `src/ai.js` (OpenRouter, `HOTPICK_AI_MODEL` default gpt-4o-mini) — first prompt over-vetoed (37/37, citing thin-H2H/threshold-edge non-reasons); rewritten with named red-flag criteria + full aggregates → 16 confirmed / 21 vetoed with substantive reasons
- [x] Config: `HOTPICK_*` thresholds (defaults imported from goals-rules — no drift) + `OPENROUTER_API_KEY`/`OPENROUTER_URL`/`HOTPICK_AI_MODEL` in `src/config.js` + `.env.example`; real key in gitignored `.env` only
- [x] Pipeline steps 10 (predictions) + 11 (hotpicks), `STEPS = 11`; `runDateRefresh` gains both for today/future dates; CLI actions `predictions` / `hotpicks`
- [x] Read layer: `fixture_predictions` LEFT JOIN (1:1, count-safe) → row fields `hot`/`hot_score`/`hot_outcome`/`hot_reason`/`hot_signals`; `hot`+`hot_score` in BASE_FIELDS (sortable/filterable, FilterBuilder picks them up automatically); `GET /api/hotpicks` summary endpoint
- [x] Web: 🔥 badge on fixture cell (🔥✓/🔥✗ once settled, tooltip = AI reason or signal audit), header accuracy chip (30d hit-rate, else pending count); `npm run build:web` clean
- [x] Verify: migration on populated DB; backtest table prints; predictions fetch-once; hotpicks idempotent (16 hot stable across reruns); server endpoints + `hot eq 1` filter live-checked via browser (badges + chip render); orphan-free (verify server stopped)
- [x] Settlement observation (closed 2026-07-04): first 9 picks settled (8 tips) via `results` → `hotpicks`; freeze verified LIVE against a pre-settle snapshot of all 27 past-kickoff ledger rows — 26 settle-column writes, every one NULL→value, 0 frozen-field violations, 0 rewrites (navbar chip was removed in 12d; the scoreboard lives in `performance`/`GET /api/performance` now)

## Phase 12b — "Tip" column + completed-games toggle (added 2026-07-03)
- [x] Migration `20260703000002_tips.js` — `fixture_predictions` += `tip_market/tip_price/tip_confidence/tip_outcome` (tip_outcome owned by settle, like outcome)
- [x] `src/db/tip-rules.js` (pure, zero imports) — `teamOutcomeAggregates`/`h2hOutcomeAggregates` (W/D/L + per-line over rates), `bestTip` (safest bettable outcome across 1X2/DC/all O/U lines; confidence = devigged market 0.6 + stats 0.3 + API percents 0.1, renormalized; DC probs derived from the devigged 1X2 book; price floor `TIP_MIN_PRICE` 1.2 excludes junk odds — the hidden-gem mechanic; `TIP_MIN_CONFIDENCE` 0.5 floor), `tipHit` (settles any canonical market)
- [x] Tests `tests/tip-rules.test.js` — 13 tests (aggregates, devig, price floor, DC derivation, stats corroboration both directions, thin-sample neutrality, O/U complement, API blend, floors, tipHit matrix). Suite 53/53
- [x] Writer: `_loadMarkets` generalizes odds loading to all canonical groups via `marketKey` (per-group provider preference — no cross-provider devig); tips computed beside hot gates; tip settle pass (pure tipHit, grouped whereIn updates); PICK_COLUMNS extended
- [x] Read layer: `tip` base column (sorts/filters by `fp.tip_confidence`) + `tip_*` row fields; web `Tip` column right of Goals (`🔥 O 2.5 · 74%` style, ✓/✗ once settled, price in tooltip)
- [x] "Show completed games" settings toggle (default on): `queryRecords({completed:false})` → `?completed=0` → localStorage `oddspro.show.completed`; hides terminal-status fixtures + completed matches
- [x] Verify: 53/53 offline; live run 393 evaluated / 388 tips / 16 hot unchanged (AI verdicts reused); healthy tip spread (13 markets, conf 50–84%, `12`/`O 1.5`/`U 3.5-4.5`/DC dominate); HTTP 205→117 rows with completed=0; browser-checked Tip column + toggle; stale :3001 server restarted (pid 20960 left serving)

## Phase 12c — Web UX round (added 2026-07-03, user change requests)
- [x] Missed tips render red (whole tip text + ✗; hits keep the calm ✓)
- [x] Frozen odds greyed: `available === false` (concluded / no live markets) greys ALL the row's prices like stale ones ("Frozen - betting unavailable" tooltip)
- [x] Providers future-proofed: `columnCatalog()` discovers `providers` from `matches`; "Visible providers" multi-select filters rows server-side (`?providers=a,b`); localStorage `oddspro.providers.visible` (null = all known, so newly-integrated bookmakers appear automatically)
- [x] Settings modal redesigned: three sections (Table columns / Providers / Behavior) with compact `MultiSelect.jsx` dropdowns (count summary button → checkbox panel with Defaults/All/None) replacing the checkbox grids
- [x] Pagination removed: `per_page='all'` in queryRecords (CSV export still pages at 500), whole selected date rendered at once, record-count footer, `Pagination.jsx` deleted
- [x] Column reordering: drag pills in settings (HTML5 DnD, drop inserts before target, Reset order), `applyOrder` + `BASE_COLUMNS` exported from DataTable, order persists in `oddspro.cols.order`
- [x] Verify: 53/53 offline; API checks (205 rows unpaged, betpawa-only 102, catalog providers); browser-checked modal, frozen-grey FT rows, saved order `[fixture, tip, start_time]` renders first; server restarted (pid 11584)

## Phase 12d — Web UX round 2 (added 2026-07-04, user change requests)
- [x] Navbar decluttered: hot-picks accuracy chip removed entirely (`fetchHotpicks` + state/effect/memo deleted; row-level 🔥 badges remain); date-picker calendar icon inverted via `.date-input-dark::-webkit-calendar-picker-indicator` (was near-black on the dark navbar)
- [x] Client-side sorting for EVERY column, descending first (desc → asc → off; shift-click multi-sort kept): new `web/src/sortValues.js` registry — form "WWDLW"→points (W3/D1), h2h "2W-1D-0L"→points, rolling-goals "gf/ga (avg)"→parsed avg (format cross-referenced with `formatGoals`), score→total goals, `fs:*` "H / A"→sum, tip→confidence + (hot ? 1 : 0) so 🔥 picks top desc, nulls always last; `sort` param dropped from `fetchRecords` (header clicks never hit the network; server sort untouched for CSV)
- [x] New columns: `Updated`/`Locked` base timestamp columns (row `locked_at` = `m.completed_at` surfaced; both in BASE_FIELDS → filterable), `Season`/`Round` stat columns beside League (`f.round` newly selected; both default on, so also in CSV)
- [x] Per-column cell tooltips (`CELL_TITLES` registry; row-level "Updated …" title removed): API ID → canonical `fixture_api` name (new `teams` join) + league·season·round; Score → start/teams+goals/timestamp; Goals → split score+timestamp; H2H+Meetings → recent matchup list from new `h2h_meetings` row field (derived from the already-fetched H2H rows, `h2hSummary` rules, newest-first, capped 10, "+N more"); Status → glossary; headers → definitions + abbreviated labels (`ID`, `Mtgs`, `H:GvO`…) via `HEADER_META`
- [x] Sticky chrome: scroll container `overflow-auto max-h-[calc(100vh-8.5rem)]`; header row `sticky top-0`; Score stays in its natural spot (left of Goals, drag-order honored) and a `sticky left-0` DUPLICATE pin appears only while the real column is scrolled out of view — hysteresis thresholds (pin at dx<0, unpin at dx≥pinWidth) because inserting the pin shifts the table by its own width (backgrounds/edge shadows on the sticky cells — opaque tints cover scrolled-under content, `group-hover` tracks row hover)
- [x] Rainbow tints → two alternating tones (`bg-white`/`bg-slate-100`) per canonical fixture group; enabled-but-empty market/stat columns omitted from render (base columns always show)
- [x] Date survives refresh via URL (`?date=YYYY-MM-DD`, `?date=all` for cleared view, clean path for today; lazy init + `popstate` for back/forward; `changeDate` wraps all navigation)
- [x] FilterBuilder: rows right-justified (`justify-end`), Enter applies, and column-to-column comparisons — `val`/`col` RHS toggle swaps the value input for a column select; wire shape `{key, op, col}` resolved server-side via `_sqlTarget` with identifier binding (`??`), `like`+col rejected 400
- [x] Verify: 53/53 offline; build clean; temp :3999 server smoke — row carries `locked_at/season/round/fixture_api/home_team/away_team/h2h_meetings` (≤10), `1 lt col 2` → 119/205 rows (0 violations), bad col / like+col → 400; Playwright-driven UI — desc-first cycle, 🔥-topped Tip sort, nulls last, 0 extra network on sort, sticky both axes (geometry + screenshot), two-tone rows, URL round-trip incl. back/forward, filter col-mode applied `1 < 2` → 57 records; temp server killed (orphan-free)

## Phase 13 — Prediction refinement: eligibility, justification, web-grounded AI, ROI (added 2026-07-04)
Objective: weed out false positives for profitable-ROI consistency. Decisions (user, 2026-07-04): skip ineligible fixtures WITH stored reason; justification = click popover + short tooltip; AI = web-grounded (`:online`) review of hot picks + top tips; ROI surface = API + CLI only.
- [x] Step 0: memory files + implementation-plan.md + docs/memory-bank.md synced (this entry)
- [x] Step 1: `tipEligibility()` pure gate in `src/db/tip-rules.js` — rule body landed 2026-07-04 (user delegated the reference implementation): ONE thin side disqualifies (stats support blends both teams' evidence, so a single thin sample poisons the blend), `insufficient_history: home 2/5, away 3/5` reason detail, `no_markets` when no group exists; 5 contract tests un-todo'd, suite 70/70. First gated live run: 0 skips among 411 (history backfill guarantees ~10 games/team for snapshot-backed fixtures — the gate arms against genuinely new teams: promotions, youth, cup minnows)
- [x] Step 2: `bestTip` returns samples/effective weights/runners_up; migration `20260704000001_tip_insight.js` (`tip_breakdown` json, `tip_skip_reason`, `tip_ai_verdict/reason/model`); writer persists them (verified live: 412 tips with breakdowns, 2 `no_pick`)
- [x] Step 3: `reviewTip()` in src/ai.js (confirm/veto, fail-open, verdict-reuse keyed on tip market+price — rerun completed in 0.9s, zero re-billing); `HOTPICK_AI_WEB` `:online` flag (both adjudicators; set to 1 in .env), `TIP_AI_MIN_CONFIDENCE` (0.75), `TIP_AI_DAILY_CAP` (20); veto flags but never clears a tip. Prompt hardened twice (see memory-bank lesson 14): no unverified team news, explicit veto-direction check → 9 confirmed / 11 vetoed web-grounded
- [x] Step 4: pure `src/db/perf-rules.js` `summarizePerformance()` (7d/30d/all × tips/hot: hit-rate, flat-stake ROI, avg price + break-even, confidence/market/edge buckets, veto impact); `performanceSummary()` loader; `GET /api/performance`; CLI `performance` (all pending until first settlements; NOTE: 350/415 tips sit in the negative-edge bucket — the edge split is the profitability discriminator to watch)
- [x] Step 5: records.js exposes `tip_skip_reason`/`tip_breakdown`/`tip_ai_verdict`/`tip_ai_reason`; web TipPopover.jsx (click cell → blend rows with weights, samples, runners-up, gate audit, AI reasons; Esc/outside-click close), vetoed tips struck through, muted "no data" marker
- [x] Step 6: tests — tip-rules breakdown (2) + eligibility contract (5, todo-marked) + perf-rules (9); suite 65 pass / 0 fail / 5 todo
- [x] Step 7: docs (CLAUDE.md commands + architecture, memory-bank lesson 14), verification (migrate batch 6, live runs, Playwright popover check on :3999 temp server — killed, orphan-free), commit
- [x] Addendum (2026-07-04, user request): AI provider switched OpenRouter → **Google Gemini** (`src/ai.js` transport swap, same interface; `GEMINI_API_KEY`/`GEMINI_URL`, `HOTPICK_AI_MODEL` default `gemini-2.5-flash`, `HOTPICK_AI_WEB=1` now attaches the native `google_search` grounding tool). Verdict reuse now keyed on `aiModelTag()` (stored in `ai_model`/`tip_ai_model`) so model/grounding switches re-adjudicate automatically. Verdict schema tolerates a missing `reason` (Gemini omits it on some confirms). Quality vs gpt-4o-mini: tip vetoes 11/20 → 5/20 with correct direction (confirmed U 3.5 BECAUSE strikers injured), grounded dead-rubber/stakes insights (Spain U19 both-qualified veto, Lugansk geopolitics); hot picks 16 → 32 (veto rate 56% → 16%) — settlements will attribute per model tag
- [x] Addendum (2026-07-04, user request): **fairness pairing** — both teams of a fixture judged over the SAME sample, capped at the smaller side's qualifying count (`pairedTeamGoalsAggregates` / `pairedTeamOutcomeAggregates`; hot gates, tip stats AND the backtest — eligibility keeps the uncapped `pool` so skip reasons name the thin side). Backtest re-validated: shipped defaults still rank #1 at 73.3% (494 flagged/362 hits, was 73.2%/358) — fairness cost nothing. Live: 263/263 gate audits + 260/260 breakdowns carry equal samples; first real eligibility skip observed (Geoje Citizen - Seogot, `insufficient_history: away 1/5`). Suite 74/74
- [x] Addendum (2026-07-04, user request): Tip column joins Score as a left-sticky pin — single-pin logic generalized to `PIN_KEYS` with cumulative left offsets (no overlap) and a total-pinned-width unpin threshold (oscillation-free); popover works from the pinned duplicate; Playwright-verified all three scroll states
- [ ] Deferred (revisit after ~2–3 weeks of ROI data): standings-derived match-stakes signal, `/injuries` fetcher, odds-movement time series

## Phase 14 — Tip safety polish: context gate, near-Under floor, AI adjudicator v2 (added 2026-07-05)
Objective: close the sources of the first settled-loss read (516 tips, 282 settled: 74.5% hit-rate but -5.6% ROI). Failure isolation over the post-update cohort (273 settled with `tip_breakdown`) found the loss is contextual, not model-internal: `World/Friendlies Clubs` -10.63u of -15.60u (Under tips there 56% @ -10.21u), youth/reserve leagues -23.3% ROI, and near-Unders (U 2.5/3.5) -21.8% ROI even outside those leagues; U 4.5+ outside friendlies is +1.2%. **edge ≥ 0 gating REJECTED by data** (edge-positive tips 70.5% / -6.0% ROI — the phase-13 pending decision closes WITHOUT shipping). AI adjudicator found anchored ("most deserve confirmation") and contradiction-only — all settled hot vetoes re-litigated market-vs-stats numerically and were net-negative (4 winners killed vs 1 loser, -0.9u). User decisions (2026-07-05): gate near-Unders now; full structured AI review (migration + UI).
- [x] Step 1: `TIP_CONTEXT_EXCLUDE` regex + `league` input in `tipEligibility` (checked before the history screen; reason `context: friendly/youth league`); validated against all 296 warehouse league names — 21 matches, zero false positives
- [x] Step 2: `DEFAULT_TIP.minUnderLine` 4.5 — `bestTip` skips `U line` candidates below it (suppressed Unders yield to the runner-up); `TIP_MIN_UNDER_LINE` env override (config.js, .env.example)
- [x] Step 3: `hotpicks.js` passes `league` + `minUnderLine`; counterfactual on the settled cohort: context gate keeps 193 @ 78.2% / -0.8% ROI, ~+1.8% with the Under floor; pending volume kept 221/239
- [x] Step 4: perf-rules `buckets.ou_line` (per-line O/U tip slice — near-Under + O 1.5 residuals stay measurable; CLI prints it via the generic bucket loop)
- [x] Step 5: AI adjudicator v2 — migration `20260705000001_ai_review.js` (`ai_review`/`tip_ai_review` JSON); pure `src/ai-parse.js` (envelope, tolerant v2 verdict schema incl. percent-style probability normalization, grounding citations); `src/ai.js` prompts rewritten: research protocol (verify context + team news via google_search, state an INDEPENDENT probability, veto mechanically only below break-even − 0.05 margin or on a verified disqualifier; market-vs-stats contradiction alone explicitly NOT a veto ground), de-anchored (no "expected veto rate: low", blended confidence dropped from the tip prompt), `aiModelTag()` gains `#p2` so upcoming rows re-adjudicate once
- [x] Step 6: review threading — hotpicks stores/carries both JSON columns through upsert + reuse; records.js exposes `hot_review`/`tip_ai_review`; TipPopover renders probability / per-check findings / source links (fallback to plain reason for pre-v2 rows); `skipLabel` phrases the context skip
- [x] Step 7: tests — context gate (4), Under floor (2, +1 legacy adjusted to override the floor), `ou_line` bucket + shape, new `tests/ai-parse.test.js` (6); suite 87/87
- [x] Step 8: docs (CLAUDE.md, this file), memory bank, migrate (batch 7) + live verification (215 evaluated / 193 tips / 17 context skips, 0 tips on excluded leagues, 0 near-Under tips; AI hot 24/0/0, tips 8/12/0 with break-even-consistent probabilities + 10+ grounded sources per verdict; settled headline unchanged 210/72 — freeze held), web build, commits `5649bd2` + `9ad2619`
- [x] Addendum (2026-07-05, user request): **footer hit-rate scoreboard + full-catalog filter options** — the footer under the table shows settled 🔥 O2.5 and Tip hit rates (`hits/settled (pct%)`, counted once per canonical fixture — provider rows duplicate the same `fixture_predictions`; AI-vetoed tips included, `—` when nothing settled) beside the record count (`X of Y records` while client filters narrow the set). FilterBuilder now lists EVERY catalog column in Base/Markets/Stats optgroups: server-filterable conditions (base + markets) still run in SQL; derived STATS/`score` conditions filter client-side over the loaded selection via new pure `web/src/filterValues.js` (comparison ops reuse `sortValue` so filtering agrees with sorting — form→points, `"gf/ga (avg)"`→avg, `fs:*`→H+A sum; `like` matches the raw text; missing values never match; col-mode goes client when EITHER side is client-only; editing a client-only filter never refetches — the records effect keys on the serialized server subset). No backend changes. Tests `tests/filter-values.test.js` (11) — suite 98/98; DevTools-MCP verified on 2026-07-03 data (footer 1/2 & 5/7 matched an independent dedupe recount — per-row counting would inflate tips to 14; `h2h_count ≥ 3` → 122/205 rows with ZERO new /api/records calls; hidden `fs:*` column filters fine; mixed server+client sends only the server condition — 159 server-filtered, 7 after client `like`)

## Phase 15 — Magic sort + betslip playground (added 2026-07-06)
Objective: exploit the high hit-rate for 4-leg multi-bet slips (~2.8x): rank the day's tips most-likely-to-win first so likely misses stop polluting slips. Math anchor: at avg leg price ~1.29, 4-leg break-even needs 77.4% per leg; the 359-settled read shows the structure to find it (confidence NON-monotonic with winning: 0.60-0.69 band 84% beats 0.80+ 79.2%; hot picks a NEGATIVE signal at 58.5% — the old tip-sort hot-boost was actively harmful). User decisions (2026-07-06): both milestones one build, sort first; server-side live engine (evolves as data grows, monetizable part stays server-side); best-metric = 4-leg slip survival; dropdown ranking comparator = survival → quartile rate → roi (user-selected policy).
- [x] Step 1: pure `src/db/magic-rules.js` (imports only perf-rules labelers) — `tipView` (tolerant row normalizer), `computeCalibration` (band/group/band×group-cell/O-U-line/price-band hit-rate buckets + `shrink_k`), `shrunkRate` (beta shrinkage, k=10 pseudo-counts — thin cells can't dominate), `estimateLegProb` (cell→band→confidence, clamped [0.05,0.98]), 10 `STRATEGIES` (confidence/market/stats/agreement/edge/price_band/bucket/line/cal_conf/cal_market; every scorer total via fallback chains), `scoreTip` (null = sink: tipless/skip/AI-vetoed), `magicSortRows` (stable, nulls last — provider rows stay tint-paired), `slipSummary` (odds/payout/survival/EV), `simulateStrategies` — day-grouped top-4 slip replay at real prices with **leave-one-day-out calibration** (no self-grading leak), pooled top-quarter rank-quality metric, `days>=minDays` tiering + survival-first comparator, top-5 + full-set calibration returned
- [x] Step 2: tests `tests/magic-rules.test.js` (25) — shrinkage exactness, calibration counting (vetoed counts, pending skipped), fallback chains, bucket-inversion case, sink+stability contract, hand-computed LODO leak scenario (leaked cal would flip the day-2 slip; asserts it doesn't), <legs days quartile-only, determinism, min-days degradation, empty ledger, shape, ranking policy + tier guard. Suite 123/123
- [x] Step 3: `src/magic.js` loader (`DATE_FORMAT(kickoff)` day grouping stays in the +03:00 SQL session; per-day in-memory cache, shared in-flight promise, failed compute clears) + `GET /api/magic-sort` (`?refresh=1` recomputes). Live: 359 settled / 3 days replayed, cached call 2ms
- [x] Step 4: web magic sort — `✨ Magic` header menu (`MagicMenu.jsx`, MultiSelect popover idiom: top-5 strategies with `slips a/b (pct) · top picks c/d (pct) · ROI` backtest lines, ⚠ small-sample marks, insufficient-days warning, Reset); `oddspro.magic.strategy` persists + revalidates against the fetched list; **one ordering mechanism**: picking a strategy clears column sort, any header click clears magic; DataTable branches to shared `magicSortRows` (vite `server.fs.allow ['..']` serves the out-of-root import in dev; build needs nothing — ONE scorer, no client/server drift). DevTools-verified: reorder + veto/tipless sink, precedence both ways, reload restore, unknown-id fallback, clean console
- [x] Step 5: betslip playground — header `Slips` modal (`BetslipPlayground.jsx`): pending non-vetoed tips one-per-fixture ranked by the active strategy, drag-or-+ into slip cards, stake/max-legs/min-odds limits, per-slip odds/payout/calibrated-survival/EV + limit warnings, "✨ Fill from top", `oddspro.betslips` persists per table date (date change keeps config, clears slips), gone legs greyed. DevTools-verified incl. hand-checked math (1.24×1.23×1.22×1.22 = 2.27 odds, 31.3% survival, -28.9% EV honestly displayed) and dedup/maxLegs guards
- [x] Step 6: docs (CLAUDE.md, this file, memory bank), commits `905e3ee` / `72664c0` / `72d9ec9` / `02255cf`
- NOTE: backtest honesty — top-5 selection is evaluated on the history that picked it (LODO removes calibration leakage, not selection optimism); menu copy says "Backtests, not forecasts". Only 3 replayable days exist (min_days 5 ⇒ `sufficient: false`, all ⚠) — rankings firm up as settlements accrue, revisit which strategy leads after ~2 weeks.

## Phase 16 — Web table polish: filter parity, sort hints, slips backtest, magic column (added 2026-07-06)
Objective: fix the tip-filter SQL error with full client/server filter parity, make derived sort values visible, let the betslip playground demo/backtest past dates on frozen tip prices, and expose the active magic strategy's scores in the table. Spec `docs/superpowers/specs/2026-07-06-web-table-improvements-design.md`, plan `docs/superpowers/plans/2026-07-06-web-table-improvements.md`. User decisions: tip `contains` → market text / comparisons → confidence; tooltip-line sort hints only where derived ≠ displayed; full backtest with outcome grading; magic cell = `#rank · score` (raw scale, no fake percentages).
- [x] Step 1: server filter fix (`src/db/records.js`) — `BASE_FIELDS.tip` gains `like_sql: 'fp.tip_market'` (op-aware `_sqlTarget`); `_coerce(key, value, op)` throws TypeError (→400) on unparsable numeric comparisons instead of mysql2 serializing NaN as a bare SQL token (`Unknown column 'NaN'`); `like` values stay text. Live-smoke verified (like→text rows, gte→confidence, eq text→400, non-tip fields unchanged)
- [x] Step 2: client parity (`web/src/filterValues.js`) — `_raw` maps `tip` → `tip_market` so `like` matches the visible tip text; comparisons keep the sortValue (confidence + hot bonus). Test added (12/12)
- [x] Step 3: sort-value tooltips (`DataTable.jsx`) — `⇅ sorts as: <value>` line appended via the SAME `sortValue()` sorting uses, only for derived columns (form/h2h/goals-strings/score/tip/fs:*) — hint can never disagree with the ordering
- [x] Step 4: `slipOutcome(legs)` pure helper (`src/db/magic-rules.js`) — won (all hit) / lost (any miss, names broken legs — a pending leg can't save it) / open (n/m settled); legacy localStorage legs without `outcome` = pending. Tests 28/28
- [x] Step 5: slips backtest (`BetslipPlayground.jsx`) — candidate gate widens to settled non-vetoed tips (frozen `tip_price` = last-seen odds demo mode), ✓/✗ marks on candidates and legs, per-slip verdict line (WON · paid X / LOST · n legs broke it / alive · n/m settled), broken legs tinted red
- [x] Step 6: magic values column (`DataTable.jsx`) — synthetic ✨ column injected immediately left of Tip while a strategy is active: `#rank · score` (rank per unique fixture — provider rows share it; 3-decimal raw score; — for sunk rows), joins PIN_KEYS (Score/✨/Tip left-pin on scroll), header click inert (never clears magic), never enters catalog/order persistence
- [x] Step 7: verification — offline suite 127/127, build clean, API smokes (like/gte/eq-400), DevTools E2E on `?date=2026-07-04`: filter 472→26 rows all O 2.5, error banner on `tip = abc`, sort-hint tooltips render, Slips 268 settled candidates + top-4 slip graded WON · paid 214.32, ✨ column `#1 · 0.816` pinning + one-ordering invariant both ways
- [x] Step 8 (user follow-up, spec `2026-07-06-slip-autogenerate-design.md`): slip autogeneration — "Fill from top" always skips tips already on any slip, so repeated clicks generate successive distinct slips (ranks 1-4, 5-8, …) until the day's tips are exhausted (button disables; remainder slip allowed); "Hide used" toggle (persisted `hideUsed` in `oddspro.betslips` config, default ON) filters used tips from the candidate list with a hidden-count note. DevTools-verified: 3× fill = 12 distinct legs, exhaustion at 268/268, toggle restore, slip delete frees tips + re-enables Fill
- [x] Step 9 (user follow-up, spec `2026-07-06-slip-totals-design.md`): one-click full autogeneration + totals bar — "Fill from top" now drains the WHOLE unused pool in one click (successive maxLegs-sized slips, remainder last; tooltip states the count); pure `slipTotals(slips, stake)` in magic-rules (empty cards skipped, returned = WON payouts, profit = settled-only — open stakes not yet lost; tests 130/130) renders under the slips list: `N slips (w won · l lost · o open) · staked · returned · P/L` (green/red). DevTools-verified on 07-04: 1 click → 67 slips/268 tips, totals `16 won · 49 lost · 2 open, staked 6700, returned 4137.54, P/L -2362.46` (hand-checked), delete recompute + Fill re-enable. Additions: "Clear slips" button (wipes the book at once, no confirm — one click rebuilds; frees every tip so Fill re-enables) and `potential` in `slipTotals` (what the OPEN slips would pay if every pending leg landed; sky-tinted `potential X` span shown only while open slips exist)

## Phase 17 — Web SPA UI/UX: responsive, text number inputs, slips auto-mode, unified multi-sort (added 2026-07-07)
Objective: restyle the SPA for tablet/mobile (compact-icon topbar), replace the restrictive `type="number"` inputs with a text control that allows blank/partial entry without clamp-fights, fix the Slips modal (no backdrop close, z-index above sticky headers), add auto/manual slip generation with a Max-slips cap and Target-odds semantics, and merge column sorts + magic strategies into ONE prioritized sort chain. Spec `docs/superpowers/specs/…` (see plan file). User decisions: header clicks additive by default (shift = isolate); "+ New slip" prefills in both modes; "Min odds" → **Target odds** (slip closes at ≥ target, bounded by Max legs + Max slips); compact-icon topbar.
- [x] Step 1: pure helpers + tests — `web/src/numberInput.js` (`NUMBER_RE` + `clampNumber`, blank/`.`/`-`→0), `web/src/ordering.js` (`orderRows(rows, chain, columns, cal)` — one comparator over the unified chain, reusing `sortValue` + `scoreTip`), additive `buildSlips(pool, {maxLegs,targetOdds,maxSlips})` in `src/db/magic-rules.js` (existing API untouched). 19 new tests; suite 149/149
- [x] Step 2: `NumberInput.jsx` (type=text, `inputMode=decimal`; local raw string owns the display while focused so live-committed clamped values never clobber keystrokes; blank→0/min on blur) adopted for the playground inputs. Slips modal no longer closes on backdrop click (Escape + × only); Slips/Settings overlays + Magic dropdown raised `z-20/z-30 → z-40` so sticky table headers (`z-20/z-30`) never peek through
- [x] Step 3: unified multi-sort — `App.jsx` `sortChain` state persisted at `oddspro.sort` (one-time migrates legacy `oddspro.magic.strategy` string); header click additive (cycle this column desc→asc→removed, keep the rest), shift = isolate; magic menu toggles multiple strategies; `SortPills.jsx` removable priority pills (+ Clear all); Settings vertical drag "Sort priority" list (ColumnOrder DnD idiom, shown only when chain non-empty); `DataTable` orders via `orderRows`, magic column tracks the highest-priority magic entry, header superscripts show chain priority. The magic-XOR-column mutual exclusion is gone
- [x] Step 4: slips playground — Min odds → **Target odds** (autogeneration closes each slip at ≥ target, bounded by Max legs), new **Max slips** cap (0 = unlimited), **+ New slip** prefills one slip from the next unused tips (both modes) via `buildSlips`, **Auto/Manual** toggle (default manual; auto rebuilds the book on limit/pool change, debounced 200ms / 500ms max-wait; blank↔0 collapse so no extra regen; stake excluded). Candidates order via the same chain (`orderRows`) so the playground matches the table
- [x] Step 5: responsive restyle — compact-icon header (glyph always, label `hidden sm:inline`: ⟳/✨/🧾/▽/⚙/⌂; tighter mobile spacing, `md:` restores desktop), `DD-MM-YYYY` date label over a transparent native picker (min/max/showPicker/clear preserved; full-day tooltip), FilterBuilder rows wrap + full-width inputs on mobile, both modals get mobile padding, playground stacks its columns
- [x] Step 6: verification — offline suite 149/149, `build:web` clean. DevTools E2E on `?date=2026-07-04` (desktop 1280 + mobile 390): legacy magic key migrates to `oddspro.sort`; date shows `04-07-2026` + full-day tooltip; add column sort onto a magic sort → chain `[magic:edge, column:goals desc]`, pills + header superscript `2`, cycle desc→asc→removed verified; magic multi-toggle → `✨ Magic (2)`; Settings "Sort priority" list renders both entries; Slips modal = 4 text inputs (0 `type=number`), backdrop click does NOT close, Escape closes, headers don't peek through; Target odds 3 + Max slips 2 → 2 slips, combined odds [3.30, 3.80] ≥ 3, first closed early at 3 legs; Auto on + Max slips 2→3 regenerates to 3 slips after the debounce; + New slip prefilled a 3-leg slip; mobile header collapses to one icon row, Slips modal stacks vertically; zero console errors. Server stopped orphan-free (:3001 released)

## Phase 18 — Filter operators, inline sort-value prefix, settled-outcome toggles (added 2026-07-07)
Objective: extend the filter builder with `not contains` / `in` / `not in` (CSV-list values, quotes for commas/spaces); surface the hidden derived sort value inline on text columns as a `value:text` prefix; add three Settings toggles to isolate settled tip outcomes. Plan file `spicy-enchanting-spindle.md`. User decisions: prefix set = Form + H2H + STATS + Score (rolling-goals excluded, avg already shown); No miss = "clean markets + upcoming" (a single miss blacklists the whole market for the day, tipless rows drop).
- [x] Step 1: shared pure CSV parser `src/db/filter-csv.js` (`parseFilterList`, zero imports) + `tests/filter-csv.test.js` (quotes, embedded commas, `""` escape, trimming, drops). Imported VERBATIM by server + web (out-of-root, vite fs.allow)
- [x] Step 2: server ops — `records.js` `FILTER_OPS` gains `not-contains`/`in`/`not-in`; `TEXT_TARGET_OPS` diverts all four text/set ops to a base field's `like_sql` target (tip → `fp.tip_market`); `_coerceList` parses + type-coerces items (text when like_sql/string field, else numeric with NaN reject); empty list / bad number → 400
- [x] Step 3: client ops — `filterValues.js` `applyClientFilters` handles `not-contains` (negated substring) and `in`/`not-in` (`_compare===0` over `parseFilterList`, number-normalized) against the RAW value; `FilterBuilder.jsx` adds the three ops (value-only, excluded from col-mode), CSV placeholder/tooltip
- [x] Step 4: inline prefix — `DataTable.jsx` `PREFIX_KEYS` {score, home/away_form, h2h} + `fs:*` render greyed `<sortValue>:<text>` via the same `sortValue`; `_sortHint` narrowed to tip + rolling-goals (their sort value stays a tooltip)
- [x] Step 5: outcome toggles — `applyOutcomeToggles(rows,{hideHits,hideMiss,noMiss})` in `filterValues.js` (pure, tested); `App.jsx` three localStorage-persisted booleans (default off) wrap the `rows` memo after `applyClientFilters`; `SettingsModal.jsx` three checkboxes under a "Settled tips" sub-heading in Behavior
- [x] Step 6: verification — offline suite 161/161, `build:web` clean. Live API (`?date=2026-07-05`): `tip in "O 2.5","1X"` → 64 rows (only those markets); `tip not-in "O 0.5",2.5` → 306 (drops 37 tipless via NULL); `provider not-contains pawa` → betika only; market `1 in 2.10,3.00` → 2 rows priced 2.1/3 (number-normalized); bad number + empty list → 400. DevTools E2E: prefixes `3:2-1`/`8:LWDWD`/`14:4W-2D-4L` match sort value, rolling-goals `4/6 (2.0)` unprefixed; Hide hits → `Tips: 0/42`, Hide miss → `Tips: 131/131`, No miss → 11 rows / `5/5` / only clean markets (U 2.5/5.5/6.5), tipless dropped; three checkboxes render + persist; op dropdown lists all 10. Server stopped orphan-free (:3001 released)

## Phase 19 — cPanel shared-hosting deployment prep (added 2026-07-07)
Objective: prepare a portable, low-SSH deployment path to a public shared cPanel host (Setup Node.js App tool), carrying over the existing populated MySQL database, with helper scripts that keep future edits atomic and incremental. Plan file `polished-puzzling-alpaca.md`. Constraints confirmed with user: no SSH/terminal access (Setup Node.js App UI + Cron Jobs + Git™ Version Control + File Manager + phpMyAdmin only), DB <50MB (phpMyAdmin import is enough), keep the daily cron cadence, add configurable debug logging (the only observability channel without SSH is log files).
- [x] Step 1: **hard prerequisite flagged, not yet actioned by the user** — `git remote -v` still carries a plaintext GitHub PAT (flagged 2026-07-06, unrotated). Documented as the mandatory first step in `docs/DEPLOYMENT.md` before any deploy work touches `origin`.
- [x] Step 2: additive, backward-compatible env vars (`src/config.js`, all no-ops unless set) — `PORT`→`API_PORT` fallback (Passenger's port-assignment convention), `DB_POOL_MIN`/`DB_POOL_MAX` (knexfile.js, shared-hosting connection caps — cron pipeline and the always-on server are separate processes with separate pools), `DEBUG` (verbose per-step timing via new `src/utils.js#debugLog`, wired into `src/pipeline.js`'s `_step()` in both `runStartPipeline` and `runDateRefresh`), `API_TOKEN` (optional `Authorization: Bearer` guard on `/api/*`, `src/server.js` + `web/src/api.js` via build-time `VITE_API_TOKEN`, mirrored automatically by `scripts/release.js`)
- [x] Step 3 (user follow-up mid-session): `POST /api/refresh` per-date cooldown — `REFRESH_COOLDOWN_MINUTES` (default 60, `0` disables); a `Map<date, finishedAtMs>` in `src/server.js` blocks re-triggering the SAME date within the window (429 + human-readable retry time), independent of the existing single-slot `refreshJob.running` lock (unrelated concern — that one serializes actual concurrent runs regardless of date)
- [x] Step 4: `scripts/release.js` — the "atomic and incremental" release step. Gates on a clean tracked tree + `npm test` + `npm run build:web`, then snapshots `main`'s exact tracked tree (via `git archive`, never gitignored/local files) plus the fresh `web/dist` into a `git worktree` on a dedicated `deploy` branch (cPanel's Git Version Control only ever pulls `deploy` — the server never runs Vite/Tailwind). `--branch`/`--force`/`--no-push` flags
- [x] Step 5: `scripts/db-export.js` — dumps the local Docker MySQL/MariaDB DB (gzip, no `CREATE DATABASE`) for the one-time phpMyAdmin import; `scripts/pipeline-cron.sh` — Linux/cPanel cron equivalent of `pipeline-task.cmd` with an `flock` overlap guard (cPanel cron has no built-in "skip if already running", unlike the current Windows Task Scheduler default); `.cpanel.yml` — drives the "Deploy HEAD Commit" button (`mkdir` runtime dirs, `npm install --omit=dev`, `npm run migrate`, touch `tmp/restart.txt` for Passenger's restart convention); `.gitattributes` added (`*.sh`/`.cpanel.yml` forced `eol=lf` — CRLF in a shebang breaks bash on Linux)
- [x] Step 6: `docs/DEPLOYMENT.md` — full guide (prereqs/go-no-go, one-time setup order, ongoing incremental deploys, rollback, troubleshooting appendix); `README.md` pointer added
- [x] Step 7: local verification (everything possible without real cPanel access, since deployment execution itself stays manual/user-assigned) — `npm test` 161/161 throughout; `PORT`/`DB_POOL_MIN/MAX`/`DEBUG` env overrides confirmed via direct `node` smoke checks and a live `DB_POOL_MAX=1` migrate; `API_TOKEN` 401/200 verified end-to-end incl. the token actually baked into the built JS bundle; `scripts/db-export.js` run twice against the real `mariadb` container (auto-detect + explicit `--container`), gzip validated, wrong-container error path confirmed; `flock` overlap guard proven under WSL (raced two concurrent invocations — second correctly logged SKIPPED); full-stack smoke test (`npm run serve` against the prebuilt `web/dist`, `/` + `/api/columns`)
- [x] Step 8: `scripts/release.js` rehearsal (`--no-push` throughout, origin never touched) caught and fixed **three real bugs**: (1) `shell:true` on Windows word-splits multi-word git args unescaped (a commit message with spaces/parens became bogus pathspec args) — git now spawns without a shell, only `npm` (a `.cmd` wrapper, needs one) keeps `shell:true`, safe since its own args are single tokens; (2) `spawnSync('npm.cmd', …)` without a shell throws `EINVAL` — resolved by giving `npm` its own shell-scoped spawn helper; (3) `git rm -r .` followed by `git add -A` silently DROPPED `x-*-output.xx.json` (tracked on `main`, matches `**.xx*` in `.gitignore`) — re-extracted files that match a gitignore pattern look "fresh and ignored" to `add -A` without `-f`; fixed to `git add -f -A .` (safe: the worktree only ever holds exactly what `git archive` produced). All three caught by the rehearsal's own diff-stat inspection, not assumed away. Deletion-propagation and no-op/reuse-worktree paths also verified with a disposable throwaway file (added, released, deleted, re-released — confirmed the deletion reached the snapshot)
- NOTE: the actual cPanel setup, PAT rotation, first `origin/deploy` push, and DB migration remain the user's manual steps per their explicit request ("let the deployment and setup and configuration be manual") — this phase delivers the tooling + guide, not the live migration itself.

## Phase 20 — pivot to manual-first deployment (added 2026-07-08)
Objective: the Phase 19 git-based `deploy`-branch automation was never usable — cPanel Git™ Version Control needs SSH, which the `oddspro.ke` account lacks. The site was deployed successfully by **manually uploading a `dev` build** via "Create Node.js App" + File Manager. So the deploy branch is deprecated; the docs become manual-first. CI/CD is deferred until the host gains SSH.
- [x] Deprecated the `deploy` branch (it was never actually created on `origin` — only `dev`/`main` exist). Two-branch model going forward: `dev` (active) + `main` (stable). Build whichever you ship, upload the result.
- [x] Deleted `scripts/release.js` and `.cpanel.yml` (deploy-branch/Git-Version-Control automation, now obsolete). Kept `scripts/db-export.js` (DB dump) + `scripts/pipeline-cron.sh` (cron).
- [x] Rewrote `docs/DEPLOYMENT.md` manual-first: build locally (`npm run build:web` → prebuilt `web/dist`), zip-and-upload the tree (excluding `node_modules`/`.git`/`.env`/`tmp`/`logs`/`backups`), create `.env` + Run NPM Install + Restart in the Node.js App UI. Migrations without SSH via phpMyAdmin/UI script runner. Rollback = keep the previous upload zip.
- [x] Updated references: `README.md`, `.gitignore` (dropped `.deploy-worktree/`), `.gitattributes` (dropped `.cpanel.yml eol=lf`), `web/src/api.js` + `web/vite.config.js` comments (VITE_API_TOKEN now set in `.env` before build, not mirrored by release.js).
- [x] Bundled with Phase-20b UI work: configurable `VITE_GA_ID`/`VITE_APP_NAME` title + Google Analytics (build-time inject via `vite.config.js`, GA prod-only), responsive two-tier topbar redesign with viewport-anchored `MagicMenu` dropdown (fixed the flex-wrap off-screen clip), compact `D/M/YYYY` merged date-nav, and a proper `[OP]` SVG logo → `web/public/{icon.svg,icon.png,favicon.ico}` (regen via `scripts/gen-icons.js`).

## Phase 21 — in-process auto-runs, freshness signal, live minute, status bar (added 2026-07-09)
Objective: keep production (oddspro.ke, always-on Passenger app) near real time without external cron — an in-process scheduler runs a cheap LIGHT pass every 10 minutes (scores/outcomes + today's odds + link + settle picks) and the FULL pipeline once daily, connected browsers silently pick up refreshed data, plus two UI fixes (live match minute in the Status tooltip; footer → bottom-sticky status bar). Plan file `plan-for-oddspro-v1-0-1-elegant-cherny.md`. User decisions (2026-07-09): client-side distributed scraping DROPPED (browser CORS blocks cross-origin provider APIs + visitor-consent concerns); scheduler owns both light and full modes (cPanel cron demoted to optional backup); client signal = light polling of `GET /api/refresh`.
- [x] Step 1: live minute — migration `20260709000001_fixtures_elapsed` (`fixtures.elapsed` smallint unsigned null); `FixtureItem` reads `status.elapsed`, `_fixtureRows` + upsert merge persist it (covers date fetch, results settle, history backfill); `records.js` selects/returns `elapsed` (display-only, not in the filter catalog); `DataTable` Status tooltip appends `— N'` for live statuses (`LIVE_MINUTE` set; HT excluded — self-describing).
- [x] Step 2: `settleHotPicks()` extracted from `updateHotPicks()` (settle-only, no fetches/AI — light-pass callable); `runStartPipeline(days, onStep)` narrates steps + returns `{ dates, quota_remaining }`.
- [x] Step 3: env knobs (zod, `src/config.js` + `.env.example`): `AUTO_REFRESH_ENABLED` (default ON), `AUTO_LIGHT_MINUTES` (10, 0=off), `AUTO_FULL_AT` (`06:00` EAT, `off`), `AUTO_FULL_DAYS` (5), `AUTO_LOG` (ON), `AUTO_LOG_MAX_KB` (256), `REFRESH_CACHE_MINUTES` (5). Pure `src/db/auto-rules.js` (zero imports): `parseDailyTime`, `eatDateKey`/`eatMinutesOfDay` (EAT fixed offset, no DST), `isFullDue` (once per EAT day), `isLightDue`, `trimLogTail` — `tests/auto-rules.test.js`.
- [x] Step 4: `src/auto-refresh.js` — owns the single-slot `refreshJob` (moved out of server.js; extended `mode`/`dates`), `startJob()` (shared guard, un-awaited run, success-only `data_version`++ / `last_success` / per-date freshness map), `lightRefresh()`, `startAutoRefresh()`/`stopAutoRefresh()` (one unref'd 30s tick; full stamped at START — one attempt/day, no surprise sweep on mid-day restart), self-truncating `logs/auto-refresh.log`. `server.js`: POST order 403→400→409→**200 cache-fresh** (`lastFreshAt` within `REFRESH_CACHE_MINUTES`)→429 manual cooldown→`startJob` (manual-only cooldown stamp via `onFinish` — auto runs must NEVER stamp the cooldown map or today would be permanently 429'd); GET returns `refreshStatus()`; scheduler starts after listen, stops on shutdown.
- [x] Step 5: web — pure `web/src/freshness.js` `shouldReloadForJob` (+`tests/freshness.test.js`); App.jsx 60s slow poll + `maybeReload` (first observed `data_version` is baseline — no reload on mount/restart), fast 2s poll: manual completions reload unconditionally + error banner, auto completions gate silently; silent reloads skip the loading dim (`silentRef`); `{fresh:true}` → sky notice ("Already fresh — refreshed Nm ago") + reload; `DataTable` `scrollKey` prop preserves scroll across silent data swaps (key change = intentional reset); footer → fixed bottom status bar (text-xs, text-shadow, wraps; record count + day hit-rates + `⟳ HH:MM` last refresh, pulsing while running); table height budget +2rem.
- [x] Step 6: E2E verified live — light runs every minute (22s/14s), `data_version` 0→1→2, log lines landed; 403/400/409/200-fresh all observed (409 caught mid-run organically); `elapsed` served (`2H — 70'` tooltip confirmed in-browser); status bar ⟳ ticked 04:14→04:15 via slow poll; scroll held at 400 through silent reloads, no dim; mobile 390px layout clean; clean kill, port freed. Full-mode firing + trim covered by unit tests (a real sweep is expensive; boot log confirms mode labels).
- [x] Step 7: docs — `DEPLOYMENT.md` "What's new in v1.0.1" + cron demoted to optional backup (≥1h from `AUTO_FULL_AT`, cross-process deadlock warning), Passenger-residency check, pool note (one process by default), migration-by-SQL recipe incl. `knex_migrations` bookkeeping row; CLAUDE.md synced; version bumps root+web → 1.0.1.
- NOTE: light cadence spends API quota (settle refresh = pending÷20 requests/run) + provider scrapes; the quota floor guard aborts a run cleanly and the tick catch-all keeps the scheduler alive. `data_version` resets on restart → at most one benign silent reload per client.

## Phase 22 — Safety Net Protocol: safe-only picks, streak stat, rate-limit pacing, layman tooltips (added 2026-07-09)
Objective: bankroll is low — cherry-pick only the highest-probability tips for multi-bet slips ("Safe only" toggle), ground every threshold in measured performance, mitigate api-sports per-minute rate-limit deaths, and humanize the tip-justification UI. Plan file `design-a-safe-only-glittery-dragon.md`. User decisions (2026-07-09): safe ranking pinned to `market` (7-day replay leader) not dynamic; runner-up handling is SELECTION-ONLY (swap backtested net-negative: +108 rescued misses / −128 broken hits, zero covering runners-up — bestTip untouched); DEFAULT_SAFE tuned to the LODO grid leader (minParts 2, minAgreement 0.65 — the per-day rank+cap does the quality control: 94.4% legs at 2.6 picks/day vs 88.9% at 1.3/day for the stricter seed).
- [x] Step 1: `src/db/magic-rules.js` — `tipAgreement` extracted from the `agreement` scorer (behavior identical); `DEFAULT_SAFE` + `safeQualifies(row, opts)` + `safeSelection(rows, cal, opts)` (dedupe per api_id, gate, per-day rank by pinned strategy via shared `_rankCompare`, top-`maxPerDay`); tests for every gate/cap/ranking path.
- [x] Step 2: streak stat — `simulateStrategies` stats gain `streak {days, avg, best}` (depth-before-first-miss from the top of each day's ranking; display-only, ranking policy unchanged); shape-contract + empty-ledger tests updated; MagicMenu shows `streak avg/best` with a plain-language legend tooltip. Live: `agreement` streak avg 4.3 / best 11 — confirms the user's observation.
- [x] Step 3: web Safe-only toggle — `oddspro.show.safeOnly` (noMiss pattern end-to-end), safe set computed day-level over `result.data` (stable under other toggles), table filters by api_id membership (provider rows + tints intact), footer `🛡 Safe: N` day-level count, BetslipPlayground inherits the filtered pool with zero changes.
- [x] Step 4: api-sports per-minute rate limits — pure `src/db/rate-rules.js` (`minuteRemaining`, `msToNextMinute`, `isRateLimitError` — daily-quota message deliberately excluded, `shouldRetryRateLimit`); `_getPage` paces on the `x-ratelimit-remaining` header and retries `errors.rateLimit` (bounded, 2) instead of dying mid-backfill; `tests/rate-rules.test.js` incl. the observed live error shape.
- [x] Step 5: `scripts/analyze-safe-tips.js` (weekly re-run BEFORE touching DEFAULT_SAFE) — LODO gate grid (strategy × agreement × parts × price × cap → leg rate, picks/day, 2/3/4-leg slip survival, shipped combo marked `*`), runner-up hypothesis re-tests (swap/close-gap/covers/direction), strategy recap incl. streak; `settledTipRows()` exported from `src/magic.js` (loader + script share one query, now carries fh/fa).
- [x] Step 6: layman tooltips — TipPopover rewritten in plain words ("Why this tip", signals as bookmaker odds / recent form / expert data with %-of-verdict weights, SIGNAL_LABEL glossary for the over-2.5 gate audit, spelled-out AI verdicts, 🛡 Safe-pick badge via `safeQualifies`); DataTable 🔥 badge tooltip uses the same glossary; docs/safety-net-protocol.md (honest bankroll-recovery protocol: 2–3 leg slips only, flat 1–2% stakes, zero-picks-means-no-bet, variance expectations, rejected hypotheses recorded).
- [x] Step 7 (user follow-up): safe policy configurable via `.env` — `SAFE_MAX_PER_DAY`/`SAFE_STRATEGY`/`SAFE_MIN_PARTS`/`SAFE_MIN_AGREEMENT`/`SAFE_MAX_PRICE` (zod, `config.js`, defaults = `DEFAULT_SAFE`). The browser can't read `.env`, so `src/magic.js` ships the resolved `safe` object in the `/api/magic-sort` payload and `App.jsx` passes it into `safeSelection` (server + client can't diverge; effective on server restart, no web rebuild). Footer/Settings copy interpolate the live cap. Verified live: `SAFE_MAX_PER_DAY=2` → payload `maxPerDay:2` → browser `🛡 Safe: 2` + Settings "best 2 per day".

## Phase 23 — iOS tablet UI redesign (added 2026-07-09/10)
Objective: reskin `web/` to a native iPadOS look AND touch-first feel (light + dark), **presentation-only — zero functional regression**. Anchored to the user's own validated Claude Design prototype "Odds Pro Tablet v3" (project `719b928e-470e-4aab-af7d-0c2759f36d5e`; chosen in-repo, NOT a Claude Design sync). Spec/plan under `docs/superpowers/` (`2026-07-09-ios-tablet-ui-redesign-*`). Merged to dev ff `130a008..a92755c`. GUARDRAILS (user): aesthetics-only, current behavior always wins, ask when unsure; kept current Score/Tip pins (NOT the prototype's ID/Start/Fixture); filter chrome-only (enhancements deferred to a future session).
- [x] Step 1: theme token layer — `web/src/index.css` Tailwind 4 `@theme inline` + CSS vars flip light/dark on `prefers-color-scheme` + `:root[data-theme]` override (wins both ways); `-apple-system,…,Inter Variable`; `op-*` keyframes; `.border-hairline`.
- [x] Steps 2–4: foundation — `icons.jsx` (SVG line icons + spinner, `currentColor`), `Logo.jsx` (inline `[OP]` via `--logo`, home→today), `Sheet.jsx`+`SheetClose` (centered sheet, blur scrim, Escape/backdrop dismiss).
- [x] Step 5: App shell + nav + footer + overflow — `h-[100dvh]` flex shell (only the table scrolls); distinct translucent nav bar; refresh button swaps icon→spinner + step/last-refresh in tooltip; responsive **⋯ overflow menu** (`OverflowMenu.jsx`) below `sm`; footer = flex child, 4 items, whole-item wrap (refresh removed).
- [x] Step 6: `CalendarPopover.jsx` — custom month grid (bounds/disable/Clear/Today, noon-anchored) replacing the native date input.
- [x] Step 7: Magic → iOS sheet (`MagicMenu.jsx` refactored to sheet body; stats/legend/clear preserved).
- [x] Step 8: `SortPills.jsx` → iOS chip strip.
- [x] Step 9: Settings/Help wrapped in `Sheet`; Filters moved inline→sheet (controls/ops/logic verbatim); Betslip playground restyled in its own sheet-styled container (keeps no-backdrop-close). All controls/persistence/`buildSlips`/EV/totals unchanged.
- [x] Step 10: `MultiSelect.jsx` iOS dropdown + accent checkboxes (`NumberInput.jsx` styled by callers).
- [x] Step 11: `DataTable.jsx` restyle via token cheat-sheet — **Pins (Score/Tip)+hysteresis, sort/`orderRows`, `scrollKey` scroll preservation, magic column, all cell logic UNCHANGED**; container `flex-1 min-h-0`.
- [x] Step 12: `TipPopover.jsx` → iOS card (blend/runners-up/gate-audit/AI verdict/skip reasons/🛡 badge preserved).
- [x] Step 13: `Tooltip.jsx` bubble → themed surface card (tap-to-toggle touch logic already existed).
- [x] Step 14: verify — `npm test` 185/185 (no shared pure module touched), `build:web` clean, DevTools E2E light+dark at iPad portrait (834) + phone (390): theme flip, calendar, overflow, magic sheet, table, tip popover, all four sheets, fill-from-top (13 slips), sort pills.
- Brand/icons: `[OP]` purple `#5856dc`, accent `#007AFF`; app icons rebranded (square badge `web/public/{icon.svg,icon.png,favicon.ico}`, 7 source variants in `web/icon-svgs/`, rasterized via throwaway `sharp`).
- NOTE: live site (oddspro.ke) needs a MANUAL `web/dist` upload to publish the redesign (per `docs/DEPLOYMENT.md`).

## Phase 24 — web theme + filters + touch UX pass (added 2026-07-10)
Objective: warmer/more readable theme, a real theme switcher, honestly date-dynamic column & filter controls, a filter overhaul, touch-first betslip, and toolbar-anchored modals — **presentation/front-end only, no pipeline/DB/API contract change**. Plan `scalable-wibbling-koala.md`; branch `feat/web-phase24-theme-filters-touch`. User-locked: teal accent (#0FB5A6/#17C9BA, links de-blued), warm-slate dark, System/Light/Dark (System default), tap-to-assign betslip, "Thu, Jul 9" dates; mid-flight asks: unclip filter dropdowns, anchor modals near the actions, sticky modal summaries, lighter scrim, favicon zoom-fit.
- [x] Step A: theme foundation — `index.css` warm-slate dark + teal accent (light+dark, `color-scheme` pinned per theme + `select`/`option` legibility rule), table radius `rounded-2xl`→`rounded-xl`, `IconGear`→ real cog, FOUC guard in `index.html`, links de-underlined.
- [x] Step B: theme switcher — `web/src/theme.js` (get/set/apply, System removes `data-theme`) + Settings **Appearance** segmented control, `App.jsx` state.
- [x] Step C: date-dynamic selectors — `web/src/columns.js` `availableColumnKeys(rows)` (+`labelFor`, `BASE_COLUMNS`→ pure `web/src/baseColumns.js`); `MultiSelect` gains `availableKeys` (non-destructive to hidden picks); App feeds it to Settings + FilterBuilder.
- [x] Step D: filter overhaul — grouped/normalized fields, list-value pickers (league/status/provider/season/round via `distinctValues`; `in`/`not-in`→`MultiSelect`, `eq`/`ne`→select), live "N of M match", `league` client-side (`CLIENT_ONLY_KEYS`), `toFilterCsv`; **`MultiSelect` panel `position:fixed`** so no ancestor overflow clips it.
- [x] Step E: Settings reorg (Appearance·Columns·Sorting·Providers·View&tips) + shared pointer-drag `DraggablePills.jsx` (Column order + Sort priority) + configurable **Safe limits** (`oddspro.safe.overrides` → `safeSelection` opts, no server change).
- [x] Step F: touch — betslip tap-to-assign (prominent left ＋, active-slip "ADDING HERE", desktop drag kept) + sticky totals footer; ≥44px tap targets; nav date "Thu, Jul 9".
- [x] Step F2 (mid-flight): modals anchored top-right (`Sheet.jsx` + betslip, footers pinned, inner `max-h`→`calc(100dvh-4.5rem)`); scrim → `bg-black/15`+`blur(1px)`; favicon `icon.svg` zoom-fits "OP".
- [x] Step G: tests + verify + docs — `tests/columns.test.js` + `distinctValues`/`toFilterCsv`/league cases → suite **192/192**; `build:web` clean; DevTools E2E on 07-09 desktop+mobile, light+dark (theme switch, dynamic Stats 13/32, league `in` picker unclipped→6/136, anchored Settings/Filters/Betslip, sticky P/L, reduced scrim).
- NOTE: raster favicons (`.ico`/`.png`) NOT regenerated — no local SVG rasterizer (sharp/ImageMagick absent); the `.svg` is the primary tab icon. Live site still needs the MANUAL `web/dist` upload.

## Phase 25 — data-fix rerun + input/betslip UX (2026-07-10)
Objective: apply the latest logic to ongoing/upcoming fixtures and repair any honestly-repairable past records; add ArrowUp/Down stepping to number inputs; retain betslips across dates. Plan `cached-coalescing-pelican.md`.
- [x] Data fix (orchestration only, no code): read-only audit (scratchpad `diagnose.mjs`) found **ZERO honest backlog** — settle / completion / deep-stats all 0; the freeze-and-settle design + auto-refresh keep the past honest. Stopped the local `serve` (scheduler) to avoid a two-process odds-write deadlock, ran `npm run start` (today+3): 347 upcoming re-snapshotted + re-evaluated (27 hot, 265 tips), standings 1764 rows, quota 149k/150k; restarted serve. Re-audit clean, `performance` scoreboard intact. Frozen past prematch/tip/hotpick SELECTIONS deliberately NOT recomputed (would fabricate pre-kickoff state / corrupt the honest scoreboard); 46/141 coverage gaps + ~4.8k uncorrelated past matches are structural, left alone.
- [x] NumberInput ArrowUp/Down stepping — pure `stepNumber` (default 1 int / 0.1 else; per-field override: stake 1, Safe min-agreement 0.05; snaps to step precision, clamps via `clampNumber`; shared `parseRaw`) + `onKeyDown` in `NumberInput.jsx`; `type=text` inputs had no native stepping. +4 offline tests.
- [x] Cross-date betslip retention — dropped the `_loadSlips` date gate so slips survive date changes (accumulate tips across days); legs are self-contained, cross-date legs show a date tag, greyed "gone" reserved for tips off today's view; only the user empties the book.
- [x] Verify — `npm test` **196/196**, `build:web` clean, DevTools E2E on 07-10: real ArrowUp steps 2.4→2.5; **14 saved slips retained** after switching to 2026-07-09 (would've cleared pre-change); no console errors.
- NOTE: live site still needs the MANUAL `web/dist` upload to publish. The local `serve` was restarted under this session — the user can re-run `npm run serve` in their own terminal if preferred.

## Phase 26 — provider priority + one-of-each + control tooltips (2026-07-10)
Objective: make bookmakers sortable by priority and add a one-row-per-game view that complements providers, plus tooltips on controls. Same session as Phase 25.
- [x] Provider priority control — `web/src/components/ProviderPriority.jsx` (fixed-anchored dropdown like `MultiSelect`; per-bookmaker enable checkbox + ↑/↓ arrows) replaces the visible-providers multi-select. App holds priority order (`oddspro.providers.order`) + enabled set (`oddspro.providers.visible`); `selectedProviders` = enabled in priority order; `orderedProviders` declared BEFORE the `rows` memo (the one-of-each dedupe reads it — a later `const` caused a TDZ blank-screen until moved up).
- [x] One of each — `oddspro.show.oneEach`; pure `applyOneOfEach(rows, orderedProviders)` in `filterValues.js` collapses to one row per `api_id` from the highest-priority ENABLED provider present (games only a lower-priority provider carries still show). +5 offline tests.
- [x] Tooltips — `title` on the market/stat/link selectors (`MultiSelect` gains a `title` prop), theme buttons, every View & tips toggle (`Toggle` gains `title`), the provider control, Reset/Done. Nav buttons already had titles.
- [x] Verify — suite **201/201**, `build:web` clean, DevTools E2E on 07-10 (reorder betika→top persists to `oddspro.providers.order`; one-of-each 165→**91/165** unique fixtures; no console errors).
- NOTE: the "Min signals resets to 3" report was NOT a bug — the code default is 2 (`DEFAULT_SAFE.minParts`/`SAFE_MIN_PARTS`); the user's `.env` sets `SAFE_MIN_PARTS=3` (served via `/api/magic-sort`), and the Settings override persists correctly (verified across close/reopen + full reload). "All past tips are hits" was the **Hide miss** toggle, not outcome leakage — `bestTip` uses only pre-kickoff inputs and freezes at kickoff; the settle pass writes only `tip_outcome`. Live site still needs the MANUAL `web/dist` upload.

## Phase 27 — active view-modifier warning pills (2026-07-10)
Objective (user, prompted by their own "all tips are hits 😅 = Hide-miss was on" confusion): warn that the table is a subset when row-hiding options are on. Same session as Phase 25/26.
- [x] `web/src/components/ViewPills.jsx` — a warning strip ("⚠ Showing a subset:") below `SortPills`, one removable chip per ACTIVE option (Upcoming only / Hide hits / Hide miss / No miss / 🛡 Safe only / One of each) + an "N filters" chip (opens the builder; `×` clears all filters). Each option chip's `×` turns it off; renders nothing when the view is unmodified. Wired in `App.jsx` after `<SortPills>`. Presentation-only (no pure logic), suite stays 201/201; `build:web` clean; DevTools E2E on 07-10 (Hide miss + Safe only → both pills shown; `×` on Hide miss cleared it + persisted `oddspro.show.hideMiss=0`; no console errors). Live site still needs the MANUAL `web/dist` upload.

## Phase 28 — unified ReorderList + settings reorg + collapsible betslip panes (2026-07-10)
Objective (user): adopt the provider-style ↑/↓ dropdown for the other reorderers, tidy the Display settings, and make the betslip fit small screens. Same session as 25–27.
- [x] `web/src/components/ReorderList.jsx` — ONE collapsed reorder dropdown (priority # + optional enable checkbox + ↑/↓ + optional × remove + optional inline tag; fixed-anchored panel like `MultiSelect`). Replaces `ProviderPriority.jsx` AND `DraggablePills.jsx` (both deleted). Providers (checkbox + order), Column order (↑/↓ + a Reset footer, `oddspro.cols.order`) and Sort priority (↑/↓ + × remove + dir tag) all share it; a `_swap` helper in SettingsModal drives `moveColumn`/`moveSort` via the existing `onOrder`/`onReorderSort`.
- [x] Settings reorg — merged the Columns + Sorting sections into one compact "Columns & sorting" row (Markets / Stats / Column order / Sort priority dropdowns), freeing the vertical space the always-visible drag lists took. No functional regression; net −69 LOC.
- [x] Collapsible betslip panes — `BetslipPlayground.jsx` Tips/Slips headers are ▸ collapse toggles; a `bodyCls` hides the body below `md` only, so one pane gives the other room on stacked small screens (desktop side-by-side unaffected).
- [x] Verify — suite 201/201, `build:web` clean, DevTools E2E: settings screenshot (4 tidy sections), Column order arrows swap + persist, Sort priority dir-tag + × + arrows, betslip 390px Tips collapse flex→none→flex. No console errors.

## v1.0.1 release — UI tweaks + deploy prep + SHIP (2026-07-11/12) ✅ DEPLOYED
Detailed ledger: `docs/v1.0.1-ui-tweaks-checklist.md` (source of truth for every A–J item). Summary:
- [x] **UI-tweak pass A–J** (dev) — A overlay/z-layer foundation, B table content cleanup, C filters overhaul (pure no-eval `web/src/filterExpr.js` recursive AND/OR + `$row[...]` engine), D betslip touch, E forms/shell uniformity, F loading/refresh UX; H deadlock-retry (`src/db/retry-rules.js`) + sticky-Tip parity + refresh simplification; I audit-extras (provider hues, 44px rows, edge-fade, empty-state, footer runner-up rates); J popup tap-away dismiss (`useOutsideDismiss.js`), compact footer, betslip full-slip guard, prediction methodology hidden behind `SHOW_INTERNALS=false`, client-clock kickoff link-disable. Plus mid-session R26–R31 (Select checkbox column + CSV export, No row-number column, per-leg market switcher, footer betting ledger).
- [x] **Phase G — deploy readiness + bot-protection.** `MIGRATE_ON_BOOT` boot-time `knex migrate:latest` (opt-in, fail-fast, pure `src/db/migrate-rules.js`) — the no-SSH migration path; opt-in bot-protection (stateless proof-of-work human gate `src/human-pow.js` + `web/src/humanPow.js`; bot-UA blocklist + AI `robots.txt` in `src/bot-rules.js`, Googlebot/Bingbot exempt) — all off by default, config in `docs/DEPLOYMENT.md §8`; G1/G2 docs (DEPLOYMENT §8, CLAUDE.md, memory-bank).
- [x] **Merged `dev`→`main` `cde3772`** (local `--no-ff`, `gh` unavailable). Suite grew to **311/311**.
- [x] **DEPLOYED to oddspro.ke 2026-07-12** — manual local `npm run build:web` → cPanel File-Manager upload (no SSH, no auto-deploy). v1.0.1 is the live version.
- [x] **Post-deploy hotfix `edddc88` on `main`:** calendar floor `MIN_DATE` `'2026-07-02'`→`'2026-07-05'` (`web/src/App.jsx`). Synced back `main`→`dev` 2026-07-12; branches re-synced dev↔main.
- Outstanding (user's manual step): **rotate the leaked GitHub PAT** in `origin`'s URL (hygiene; not a deploy blocker).

## Issues / notes
- 2026-07-02: MySQL (Docker, reachable via 127.0.0.1:3306, client seen as 172.19.0.1) denied `root` with empty password. Halted per DB-connection-failure rule. RESOLVED: user added credentials to `.env` (Laravel-style names: `DB_DATABASE`/`DB_USERNAME`/`DB_CHARSET`/`DB_COLLATION`) — config.js/knexfile.js aligned to those names.
- 2026-07-02: README rewritten with fuller spec → plan revised: fixtures = canonical base; betpawa→betika correlation order; fuzzy confidence matching + `league_aliases` cache table (added to init migration pre-first-run); Phase 6 visualization added (temp CSV export → API + React datatable).
